# Legal Plugin — Project Wiki

> Last updated: 2026-09-14 | 643 tests + 291 frontend asserts passing | Word add-in verified on **Windows** as well as Mac | Python 3.12 | Redis checkpointer active | Word add-in shipped (chat-driven edits) | Trinetix legal-team playbook integrated | Smoke-tested on real MSA / SOW / BAA | Main-flow audit + bug fixes #1/#2/#3/#4/#5 + placeholder-recall Layer 1 + Langfuse GENERATION spans & token usage + SOW-vs-MSA review (see docs/output_format_conflict.md) + chat memory & grounding + Word add-in click-to-jump & findings filter/sort + clause-locator hardening + canonical document UUID (review-store identity) + server-side per-attorney conversation store (slice 2) + O365 SSO attorney identity — backend foundation (slice 3) + chat stale-recall reconciliation + chat gate-verdict reconciliation + attorney preference memory (USER.md, stage 1) + OpenTelemetry tracing migration + repo cleanup & legal_research split + chat_history gated off contract_review + MSA-truncation & unsourced-value prompt guards + edit-block guards & preference capture + history-priming fix + pane built in Docker + fill-overwrite guard + in-flight turn logging

## What Is This

AI-powered legal assistant for an internal legal team. Attorneys submit requests through a Chainlit chat UI or via REST API. A LangGraph supervisor graph routes requests to specialized skills, retrieves grounding context via hybrid RAG search, and returns structured legal output using a local LLM.

**Fully air-gapped** — no external API calls. Everything runs locally on Mac M4 Pro (48 GB).

---

## How to Run

```bash
# 1. Start Docker services (Qdrant, Redis, Langfuse v3, ClickHouse, PostgreSQL, MinIO)
docker compose up -d

# 2. Start both backend + frontend
bash scripts/start.sh

# Or start individually with auto-reload for development:
uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
chainlit run clients/web/app.py --port 8080 --host 0.0.0.0 -w

# Access points:
#   Frontend:  http://localhost:8080  (Chainlit chat UI)
#   Backend:   http://localhost:8000  (FastAPI API)
#   API docs:  http://localhost:8000/docs
#   Langfuse:  http://localhost:3000  (admin@local.dev / admin1234)
```

### Prerequisites

- Python 3.12 (venv managed via `uv venv .venv --python 3.12`)
- Docker + Docker Compose
- Ollama running natively with models: `qwen3.6:latest`, `embeddinggemma:latest`
- llama-cpp reranker (optional, graceful fallback when unavailable)

### Demo Data

5 synthetic service agreements with clause-level variation are available:

```bash
python scripts/generate_demo_contracts.py   # saves to data/demo_contracts/ + ingests to case_history
```

CUAD dataset (510 real contracts) also available at `data/cuad/`.

---

## Architecture

```
Attorney → Chainlit (:8080) / Word add-in (:3001) → FastAPI (:8000) → LangGraph graph
                                                    ├── Skills (5 capabilities)
                                                    ├── RAG layer → Qdrant
                                                    ├── LLM → Ollama
                                                    ├── Reranker → llama-cpp
                                                    ├── Checkpointer → Redis
                                                    ├── Audit/Review/Conversation → Postgres (app-db)
                                                    └── Tracing → OTel (Langfuse local / Phoenix VM)
```

**Two-process model:**
- **Backend** (FastAPI, port 8000) — owns all business logic, graph execution, RAG, LLM calls, audit
- **Frontends** — Chainlit (:8080) and the Word add-in (:3001, Office.js task pane); both thin clients calling the backend over HTTP (`POST /api/query`)

---

## Project Structure

```
legal-plugin/
├── api/                        # FastAPI backend
│   ├── main.py                 # App entry point, lifespan, CORS
│   ├── models.py               # Pydantic request/response models
│   └── routes/
│       ├── query.py            # POST /api/query, resume, status
│       ├── documents.py        # POST /api/ingest
│       └── health.py           # GET /health
│
├── clients/                    # Client surfaces (one per delivery channel)
│   ├── web/                    # Chainlit web client
│   │   ├── app.py              # Chat, file upload, side panel, human review, PDF export
│   │   └── api_client.py       # Async HTTP client to backend
│   └── word/                   # Microsoft Word add-in (task pane, Office.js)
│       └── ...                 # manifest.xml, Vite + React/TS, see clients/word/README.md
│
├── graph/                      # LangGraph supervisor graph
│   ├── state.py                # LegalAgentState TypedDict
│   ├── graph.py                # StateGraph wiring — all nodes + edges
│   └── nodes/                  # Shared processing nodes (all @observe traced)
│       ├── intake.py           # Resolve client_id, set filters, set Langfuse trace metadata
│       ├── intent_router.py    # LLM classifies task_type, logs prompt to Langfuse
│       ├── planner.py          # LLM decomposes multi-skill requests
│       ├── skill_dispatcher.py # Routes to correct skill node
│       ├── rag_retriever.py    # Calls hybrid_search (skips if agent skill already ran)
│       ├── llm_caller.py       # Calls Ollama with context, logs prompt+response to Langfuse
│       ├── risk_assessor.py    # Citation check + risk level
│       ├── human_review.py     # LangGraph interrupt() for blocking review
│       ├── output_formatter.py # Builds structured report dict
│       └── memory_writer.py    # Writes audit log + review + conversation (Postgres)
│
├── skills/                     # Legal capabilities — each in its own folder
│   ├── base.py                 # load_skill_prompt() with ceiling constraint
│   ├── contract_generation/    # ReAct agent — searches + generates contracts
│   │   ├── contract_generation.py
│   │   └── SKILL.md            # Playbook — editable by legal team
│   ├── contract_review/        # Clause analysis with uploaded contract support
│   │   ├── contract_review.py
│   │   └── SKILL.md            # Playbook — editable by legal team
│   ├── compliance_check.py     # Policy verification prompt → shared nodes
│   ├── legal_research/         # Direct ChatOllama (doc-chat) + ReAct agent (KB research)
│   │   ├── legal_research.py   # Entry — run paths + LLM builders
│   │   ├── prompts.py          # Prompt constants (no imports)
│   │   ├── edit_parsing.py     # JSON edit-block extraction
│   │   ├── review_recall.py    # Prior-review reconciliation + gate verdict
│   │   └── context.py          # Grounding gate, prior-review/conversation load, context cap
│   └── drafting.py             # Document generation prompt → shared nodes
│
├── rag/                        # RAG layer (ported from compliance-bot)
│   ├── embeddings.py           # Ollama /api/embed
│   ├── vector_store.py         # Qdrant client — all ops take collection param
│   ├── bm25_index.py           # Pure-Python BM25 with JSON persistence
│   ├── hybrid_search.py        # RRF fusion (vector + BM25) + reranker
│   ├── reranker.py             # Multi-backend reranker (llama-cpp, vLLM)
│   └── tools/                  # LangGraph @tool decorated functions
│       ├── search_legal.py     # Hybrid search as agent tool
│       ├── get_document.py     # Retrieve full doc by doc_id
│       ├── extract_clauses.py  # Query case_history by clause_type
│       └── escalate.py         # Flag for attorney review
│
├── ingest/                     # Document ingestion
│   ├── chunk_models.py         # LegalChunk Pydantic model
│   ├── pipeline.py             # parse → chunk → embed → upsert
│   ├── numbering.py            # Word auto-numbering resolver
│   └── parsers/
│       ├── docx_parser.py      # DOCX parser (ported from compliance-bot)
│       └── pdf_parser.py       # PDF parser (pdfplumber, section-based)
│
├── memory/                     # Persistence (Postgres, `app-db`)
│   ├── db.py                   # Connection pool + init_db() (all store tables)
│   ├── audit.py                # Audit log store
│   ├── review_store.py         # Review markdown store, keyed by document_id
│   └── conversation_store.py   # Per-attorney chat history store
│
├── observability/              # Tracing
│   └── langfuse.py             # init_observability() — sets env vars for @observe
│
├── scripts/                    # Utility scripts
│   ├── create_collections.py   # Create 3 Qdrant collections
│   ├── generate_demo_contracts.py  # Generate + ingest 5 synthetic contracts
│   ├── ingest_all.py           # Batch ingest from directory
│   ├── start.sh                # Start backend + frontend
│   ├── test_api.sh             # Curl-based API verification
│   ├── test_contract_gen.py    # Contract generation e2e test
│   └── test_graph_flow.py      # Graph routing verification
│
├── config.py                   # pydantic-settings — single config import
├── docker-compose.yml          # 7 services (Qdrant, Redis, Langfuse v3 stack)
├── requirements.txt            # All Python dependencies
└── tests/                      # 64 tests, 13 test files
```

---

## Skills Architecture

### SKILL.md Pattern

Each skill lives in its own folder with two files:
- `skill_name.py` — the Python function
- `SKILL.md` — the playbook, editable by the legal team

The playbook is loaded via `load_skill_prompt()` from `skills/base.py`, which wraps it with a **ceiling constraint**: the LLM will ONLY follow the playbook instructions, never improvise from its pretraining knowledge. If the playbook doesn't cover something, the LLM says "Not covered by current playbook" and stops.

This means: **the legal team controls exactly what the LLM outputs by editing SKILL.md**. No code changes needed.

### Skill Types

**Agent Subgraphs** — have their own tools, handle retrieval and LLM calls internally:

| Skill | Tools | What it does |
|---|---|---|
| **contract_generation** | search_legal, get_document, extract_clauses, escalate | Searches case history for clause patterns, generates contracts with deviation report. Always → human_review. |
| **legal_research** | search_legal, get_document, escalate | Multi-hop retrieval to answer legal questions. Cites sources, flags conflicts. |

**Plain Skills** — set a system prompt, then the shared `rag_retriever` + `llm_caller` nodes handle retrieval and generation:

| Skill | What it does |
|---|---|
| **contract_review** | Clause-by-clause analysis with GREEN/YELLOW/RED severity. Supports uploaded contract files (PDF/DOCX/TXT). |
| **compliance_check** | Policy verification prompt → shared nodes. |
| **drafting** | Document generation prompt → shared nodes. Always → human_review. |

### Agent vs Plain Skill — Graph Behavior

For **agent skills** (contract_generation, legal_research): the skill function calls the LLM and tools itself, sets `llm_response`. The shared `rag_retriever` and `llm_caller` nodes detect this and skip.

For **plain skills** (contract_review, compliance_check, drafting): the skill function sets `messages` (system prompt from SKILL.md + user request). The shared nodes then do retrieval and call the LLM with those messages.

---

## Graph Flow

```
Attorney sends request
         │
         ▼
    ┌──────────┐
    │  INTAKE   │  Resolves client_id from user_id
    │           │  Sets filters (client_id always present)
    └────┬─────┘  Sets retrieval_query = request
         │        Sets Langfuse trace metadata (user_id, session_id)
         ▼
  ┌──────────────┐
  │ INTENT_ROUTER│  Calls Ollama to classify task_type:
  │              │  contract_generation | contract_review | compliance
  └──────┬───────┘  research | drafting
         │          Falls back to "research" if LLM unavailable
         │          Logs prompt + classification to Langfuse
         │
    ┌────┴────┐
    │         │
 (multi)  (single)
    │         │
    ▼         ▼
┌────────┐ ┌──────────────────┐
│PLANNER │ │ SKILL_DISPATCHER │  Routes to skill node by task_type
└───┬────┘ └────────┬─────────┘
    │               │
    └───────┬───────┘
            ▼
   ┌────────────────┐
   │  SKILL NODE    │  One of 5 (see Skills Architecture above)
   └───────┬────────┘
           │
           ▼
    ┌──────────────┐
    │ RAG_RETRIEVER│  hybrid_search() with client_id filter
    │              │  Skips if agent skill already set llm_response
    └──────┬───────┘
           │
           ▼
     ┌───────────┐
     │ LLM_CALLER│  Ollama /api/chat, temperature=0.0
     │           │  Uses skill-provided messages or default prompt
     └─────┬─────┘  Skips if agent skill already set llm_response
           │        Logs full prompt + response to Langfuse
           ▼
    ┌──────────────┐
    │RISK_ASSESSOR │  Checks if LLM response cites sources
    │              │  No citation = high risk → human_review
    └──────┬───────┘
           │
      ┌────┴────┐
      │         │
  (high risk   (low risk)
   OR contract_gen
   OR drafting)
      │         │
      ▼         ▼
┌────────────┐ ┌─────────────────┐
│HUMAN_REVIEW│ │OUTPUT_FORMATTER │
│ interrupt()│ │                 │
└─────┬──────┘ └────────┬────────┘
      │                 │
   route_review:        │
   loop_back ─→ skill_dispatcher (revise with previous_draft + notes)
   terminal  ─→ output_formatter
                        │
                        ▼
              ┌─────────────────┐
              │ HISTORY_APPENDER│  Appends turn to chat_history (capped)
              └────────┬────────┘
                       ▼
                ┌──────────────┐
                │MEMORY_WRITER │  Writes to Postgres audit_log table
                └──────┬───────┘  Every invocation, no exceptions
                       │
                       ▼
                      END → Response to attorney (or interrupt_payload if paused)
```

### Routing Rules

| Condition | Route |
|---|---|
| `task_type` = contract_generation | **always** → human_review |
| `task_type` = drafting | **always** → human_review |
| `risk_level` = high or medium | → human_review |
| `risk_level` = low | → output_formatter (skip review) |
| `skill_plan` has multiple skills | → planner first |

### human_review verdicts (after resume)

`route_review` reads state after `human_review` returns and decides whether to exit or loop:

| Resume payload | What `human_review` does | Where `route_review` routes |
|---|---|---|
| `approved=True` | Saves notes, clears `awaiting_review` | → output_formatter (terminal approve) |
| `revised_response` non-empty | Replaces `llm_response` with revised text | → output_formatter (terminal revise) |
| `notes` only + `iter < MAX_REVIEW_ITERATIONS` | Stashes `llm_response` into `previous_draft`, clears `llm_response`/chunks/messages, `iter += 1` | → skill_dispatcher (loop-back) |
| `notes` only + `iter ≥ MAX_REVIEW_ITERATIONS` | Sets `report_notes_unincorporated = notes`; clears `awaiting_review` | → output_formatter (terminal cap-hit) |
| `approved=False`, no notes (pure reject) | Clears `awaiting_review` | → output_formatter (terminal reject) |

On loop-back, the contract skill detects `previous_draft + attorney_notes` and runs a single direct LLM call (`reasoning=False`) instead of the full ReAct agent — much faster.

---

## Chainlit Frontend

### Chat Features
- Send legal queries, get responses with task type and risk level
- **File upload with message** → review/analyze the uploaded contract (PDF/DOCX/TXT)
- **File upload without message** → ingest into Qdrant knowledge base
- Contract generation/review results shown in **side panel** (click attachment to view)
- Each result gets a unique filename — all previous results remain clickable
- **PDF export** — approve a contract draft and download as PDF

### Human-in-the-Loop
- Contract generation and drafting always show review buttons
- Three actions: **Approve Draft** (generates PDF), **Request Changes**, **Reject**
- **Request Changes** loops back: skill revises previous draft using attorney notes (single-shot LLM call with `reasoning=False`, not the full ReAct agent — ~5x faster), then re-pauses for review. Capped at 3 iterations; unincorporated notes attached to final report on cap.
- Uses `cl.AskActionMessage` for per-iteration review prompts so buttons stay live across loop-backs (Chainlit disables same-named `cl.Action` after click — `AskActionMessage` blocks inline with fresh buttons each round).
- Session state persisted via RedisSaver checkpointer with 24h TTL refresh on every interaction.

### Development Mode
- Web client: `chainlit run clients/web/app.py -w` (auto-reload on file changes)
- Backend: `uvicorn api.main:app --reload` (auto-reload on file changes)

---

## Observability

### Langfuse Tracing (http://localhost:3000)

All graph nodes are instrumented with `@observe` from the Langfuse SDK. Every request creates a trace with:
- **Nested spans** for each graph node (intake → intent_router → skill → rag_retriever → llm_caller → ...)
- **GENERATION observations with token usage + model** on every LLM call. The httpx nodes (`llm_caller`, `intent_router`, `planner`) mark their span `as_type="generation"` and attach Ollama's `prompt_eval_count`/`eval_count` (via `observability/tracing.py::ollama_usage`). The skill LLM calls — the Word chat-tab direct `ChatOllama` (`legal_research._run_doc_chat`), its `format='json'` retry, and both ReAct agents — run inside `traced_invoke`/`traced_agent_invoke`, which record a nested GENERATION with usage read from the returned `AIMessage.usage_metadata`. These were previously **invisible**.
- **Skill spans** — all five skills (`legal_research`, `contract_generation`, `contract_review`, `drafting`, `compliance_check`) carry their own `@observe` span (`capture_input/output=False` so the large state isn't dumped).
- **User and session tracking** (user_id, session_id from the request)
- **Task type tags** for filtering in the Langfuse UI
- **Model metadata** (model name, temperature, chunk count)

No LangChain dependency — uses Langfuse native Python SDK (`@observe` decorator + `langfuse_context`). The SDK is **v2** (`langfuse>=2.0,<3.0`); it reports into the **v3 Langfuse server** (`langfuse/langfuse:3`) over the backward-compatible ingestion API. Token **cost** stays null for local Ollama models unless a model price is registered in the Langfuse UI (Settings → Models). NB: Langfuse's *LangChain* callback handler is **not** usable here — it imports the full `langchain` package, which isn't a dependency (only `langchain-core`/`langchain-ollama`) — hence the manual `traced_invoke` wrapping.

### Postgres Audit Log (app-db)

Every skill invocation writes to `audit_log` table: timestamp, session_id, user_id, skill_name, task_type, request_summary, risk_level, review_status, duration_ms.

### Phoenix (http://localhost:6006)

RAG evals — shared with compliance-bot. Already running, no setup needed.

**Rule: Langfuse = agent traces. Phoenix = RAG evals. Don't mix.**

---

## RAG Pipeline

```
Query → embed_query() → Qdrant vector search
                       ↘
                        RRF fusion (k=60) → reranker → top N results
                       ↗
        BM25 keyword search (if enabled)
```

### Qdrant Collections

| Collection | Content | Chunking |
|---|---|---|
| `legal_docs` | Contracts, legislation, templates, policies | Varies by doc_type |
| `case_history` | Past signed contracts (5 demo + CUAD) | Clause-level (one chunk per clause) |
| `memory` | Attorney preferences (stub — future) | Small entries |

### Ingest Pipeline

```
Document (PDF/DOCX) → Parser → Chunks (LegalChunk) → Embed (Ollama) → Upsert (Qdrant)
```

- **DOCX parser** — heading detection, clause numbering, Word auto-numbering, table-to-text, chunk splitting/merging, noise filtering
- **PDF parser** — section-based chunking by heading regex
- **Batch ingest**: `python scripts/ingest_all.py <dir> --collection legal_docs --client-id <id>`

---

## API Endpoints

| Method | Path | Status | Purpose |
|---|---|---|---|
| GET | `/health` | **Working** | Health check |
| POST | `/api/query` | **Working** | Submit legal request → graph execution (supports `uploaded_text` for review). Returns `interrupt_payload` if graph pauses at `human_review`. |
| POST | `/api/query/{session_id}/resume` | **Working** | Resume after human review with `{approved, notes, revised_response}`. Iterates loop-back (request changes) until approve/reject/cap. |
| GET | `/api/query/{session_id}/status` | Placeholder | Check execution status |
| POST | `/api/compact` | **Working** | Condense this document's earlier conversation into validated verbatim quotes. Body `{document_id, reclaim_chars}` — `reclaim_chars` is how many characters the pane needs freed, measured to the **warn line** rather than the budget, and it tightens the summary without ever affecting what passes the gate; attorney comes from the auth seam, never the body. 200 with `compacted:false` + `reason` when there is nothing to condense **or when the segment would be no smaller than the rows it replaces**; **500** when the validation gate rejects the summary (and nothing is written). |
| POST | `/api/ingest` | **Working** | Upload PDF/DOCX for ingestion |
| GET | `/api/documents` | Not built | List ingested documents |
| GET/DELETE | `/api/documents/{doc_id}` | Not built | Get/delete document |
| GET | `/api/sessions` | Not built | List sessions |
| GET/DELETE | `/api/sessions/{id}` | Not built | Get/delete session |
| GET/PUT | `/api/skills` | Not built | List/toggle skills |
| GET | `/api/reviews/pending` | Not built | Pending human reviews |
| GET | `/api/audit` | Not built | Query audit log |

**Auth:** Simple `X-User-ID` header. Real auth planned for multi-user/Linux VM.

**Response envelope:** `{"status": "ok|error", "data": {...}, "errors": [...]}`

---

## Data Models

### LegalChunk (ingest/chunk_models.py)

```python
chunk_id, doc_id, doc_title, doc_filename, doc_type, client_id,
jurisdiction, sensitivity, section, section_number, clause,
clause_number, clause_type, section_display, text, char_count,
chunk_index, chunk_strategy, last_updated
```

### LegalAgentState (graph/state.py)

```python
request, user_id, uploaded_docs, task_type, skill_plan,
retrieval_query, retrieved_chunks, filters, messages,
llm_response, risk_level, risk_flags, awaiting_review,
attorney_notes, report, session_id, checkpoint_ref, trace_id,
chat_history,                  # within-session memory (capped at 2N entries via idempotent reducer)
review_iterations,             # loop-backs from human_review (capped by max_review_iterations)
report_notes_unincorporated,   # attorney notes that couldn't be applied (set on iteration cap)
previous_draft,                # llm_response from prior iteration, preserved across loop-back for revise-not-regenerate
proposed_edits,                # structured edit proposals parsed from chat skill output
contract_type_detected,        # nda | msa | sow | baa (set by contract_review when uploaded text present)
requires_attorney,             # server verdict: doc needs attorney sign-off (blocker / yellow)
interactive_review,            # caller can handle a human_review interrupt + resume (Chainlit True, Word False)
document_id,                   # stable doc id — client Office-settings UUID, preamble-hash fallback (review + conversation store key)
memory_degraded                # True when a memory read/store was unavailable this turn
```

---

## Docker Services (docker-compose.yml)

| Service | Port | Purpose |
|---|---|---|
| **qdrant** | 6333, 6334 | Vector database |
| **redis** | 6379 | Session checkpointer + Langfuse cache (shared, password: `myredissecret`) |
| **langfuse-web** | 3000 | Agent tracing UI |
| **langfuse-worker** | 3030 | Background trace processing |
| **postgres** | 5433 | Langfuse transactional DB |
| **clickhouse** | 8123, 9000 | Langfuse analytics |
| **minio** | 9090, 9091 | Langfuse blob storage |

**Running natively (not Docker):** Ollama (:11434), llama-cpp reranker, Phoenix (:6006), FastAPI (:8000), Chainlit (:8080)

---

## Configuration

All config via `.env` loaded by `pydantic-settings` in `config.py`. Import anywhere: `from config import get_settings`.

Key settings:
- `LLM_MODEL` — Ollama model name (e.g., `qwen3.6:latest`)
- `EMBEDDING_MODEL` — embedding model (e.g., `embeddinggemma:latest`)
- `QDRANT_VECTOR_DIM` — must match embedding model (768 for embeddinggemma)
- `RERANKER_ENABLED` — toggle reranker on/off
- `BM25_ENABLED` — toggle BM25 keyword search
- `DATABASE_URL` — Postgres DSN for the audit/review/conversation stores (default: `postgresql://legal:legal@localhost:5434/legal`, the `app-db` container)
- `DB_POOL_TIMEOUT` — seconds to wait for a pooled connection before a store call fails and the turn degrades (default: `3.0`). `psycopg_pool`'s own default is 30s; measured against an unreachable host, that is 30.00s of dead pane per store call versus 3.00s. `@lru_cache`'d in `get_settings` → needs a `start.sh` restart.
- `CHAT_CONTEXT_MAX_CHARS` — the doc-chat character budget (default `150000`, deliberately below `OLLAMA_NUM_CTX` × chars-per-token). This is the number the pane's gauge is a percentage of, and the only thing `_cap_chat_context` truncates when it is exceeded is the **document** — never the playbook, MSA or findings. Lowering it is the fastest way to exercise the truncation and compaction paths by hand; it is also how the fabricated-completion bug in the follow-ups below was reproduced deterministically, so put it back to `150000` when done.
- `COMPACTION_*` — `COMPACTION_ENABLED`, `COMPACTION_KEEP_RECENT_MESSAGES` (floor kept verbatim, default `2`), `COMPACTION_MIN_QUOTES` / `COMPACTION_MAX_QUOTES` (`4`/`24`), `COMPACTION_WARN_PCT` (`90` — the share of budget at which the Condense action appears, and the line the pane's reclaim target is measured to), `COMPACTION_MAX_INJECTED_SEGMENTS` (`3`), `COMPACTION_AUTO` (`true` — fire without a click), `COMPACTION_AUTO_MIN_CHARS` (`4000` — the anti-churn floor for firing unasked, which the manual button does not share; derived from segment overhead, and it must stay well under what history can actually hold or auto cannot arm until the document has already been truncated). All `@lru_cache`'d in `get_settings` → a `start.sh` restart is required. **`COMPACTION_AUTO=false` is the switch to set on a machine that is about to be demoed on**, since automatic firing puts a background 10–30s summarisation on the shared Ollama.
- `LANGFUSE_PUBLIC_KEY` / `LANGFUSE_SECRET_KEY` — pre-seeded: `pk-lf-local` / `sk-lf-local`

---

## Constraints (Never Violate)

1. **temperature=0.0** on all LLM calls
2. **SKILL.md is the ceiling** — LLM strictly follows the playbook, no improvising from pretraining
3. **Every claim must cite a source** — no citation = high risk → escalate
4. **contract_generation and drafting always → human_review** — no exceptions
5. **client_id filter always applied** on retrieval — no cross-client data leakage
6. **Every skill invocation → Postgres audit log** before returning — with one *explicit, visible* exception: when the audit store itself is unreachable, `memory_writer` writes the entry to the application log at ERROR and flags the turn `memory_degraded` (amber banner) rather than killing it. The record is never silently dropped; it moves sinks. Rationale: a turn does not need Postgres to answer, and this codebase's established answer to "the record didn't persist" is to surface it loudly (`review_persist_error`), not to withhold the attorney's work.
7. **No external network calls** — fully air-gapped

---

## Tests

598 backend tests + 279 add-in assertions, run in a few seconds:

```bash
uv run pytest tests/ -v
```

Coverage includes: graph compilation + routing + audit, end-to-end interrupt/resume/loop/cap with `MemorySaver`, Redis checkpointer factory, FastAPI endpoints (submit + resume + interrupt detection via `__interrupt__` key), all five skills with mocked agents, RAG layer (embeddings, vector store, BM25, hybrid search, reranker), DOCX/PDF parsers, ingest pipeline, and config loading.

---

## Shipped Since Last Update (2026-05-15)

| Feature | Commit / Branch | Notes |
|---|---|---|
| **Legal-team install guide + first Windows verification** | `docs/legal-team-install-guide` | `docs/tester-setup.md` was a technical tester's notes — `curl`, `sudo nano`, docker, and an operator Part A interleaved with the part an attorney follows. It is now an install guide a non-engineer can follow, one linear path per platform, with every pane label quoted from source; the tab tour and the flag/Send-feedback instructions split into `docs/using-the-assistant.md` (repointed from `feedback-loop.md`, which calls that half "the tester-facing" one) so the install guide is install steps only. Operator steps moved to `deploy-vm.md` **Step 7**, where a stale hand-build of `clients/word/dist` was corrected to `up -d --build caddy` — the bind-mount era is exactly the 2026-08-13 stale-bundle incident. **Windows (Part B) is new, and verified rather than reasoned.** B1–B4 were rehearsed on real Windows against a Mac-hosted Caddy on the LAN: `manifest.prod.xml` carries only a hostname, never an IP, so pointing that hostname at the Mac makes Part B run byte-for-byte as written with one line different, and `host.docker.internal` reaches a `127.0.0.1`-bound host backend (proven against Ollama first), so the rehearsal needed no containerised backend and no `.env` edit — `ADDIN_ORIGIN_HOST` in `.env` would in fact **crash** a locally-run backend with `extra_forbidden`, and only works on the VM because `.dockerignore` excludes `.env` and compose injects it as process env. B5, a full review and an edit apply were then confirmed on a licensed Windows machine: a 3,371-char `nda` review, `findings_rendered=4`, and **one `edit_applied` against `[Legal Name]`** — a bracketed placeholder, i.e. the exact class the `body.search` wildcard rule documents, and the first evidence the escaping/fallback behaves the same off Mac. Zero `edit_failed`/`redline_failed`. The Windows rows carry a distinct `attorney_id`, which is what makes them evidence of a separate install rather than the author's machine. **B5 is self-contained**: an earlier draft told the user "we'll send you the exact path" for the catalogue, making every install wait on an email; PowerShell now creates the folder, shares it, and writes the `TrustedCatalogs` entry itself, deriving the UNC from `$env:COMPUTERNAME`. `scripts/test-windows-install.ps1` (pwsh via `brew install powershell`, Windows-only cmdlets stubbed, deliberately **not** in `check.sh`) parses every block and pins what the prose promises a legal reader — a second run adds nothing, and a pre-existing add-in catalogue is neither edited nor removed. It caught `Join-Path` resolving the provider drive (fails off-Windows), so the code uses string concatenation — prefer the form you can test. **Three documented claims the field disproved, none findable by reading:** the guide said "look for a padlock" and the tester googled *"what is padlock icon"* (Chrome dropped it in v117 — the pass signal is now the absence of a warning); the Mac lists sideloads under **Developer Add-ins**, not the `My Add-ins → SHARED FOLDER` this doc had described, and Windows puts SHARED FOLDER behind an **Advanced** button; and `deploy-vm.md` claimed dev and prod manifests share an `<Id>` so only one could be sideloaded — they are `A8A5F2CD` / "Legal Triage (Dev)" and `D57831EF` / "Legal Triage", and a screenshot shows both installed at once. A fourth was caught by round-tripping the printed PDF: Chrome will not print the inside of a closed `<details>`, so four sections were silently absent. **Still unproven, and now the only gap:** VPN reachability to `172.20.1.10:443` from a corporate Windows laptop, and whether group policy permits the two elevated commands — a home machine can answer neither. Multi-paragraph spans, tab-separated signature blocks and `replace_all` also remain Mac-only observations. |
| **Automatic compaction** | `feat/auto-compaction` | Compaction now fires without a click. The decision is a backend field, `auto_compact` on `report.context_breakdown`, computed as `can_compact AND compaction_auto AND compressible_chars >= compaction_auto_min_chars` — built *from* `can_compact` rather than beside it, so "auto is a narrowing of the button" is structural and cannot drift. Backend rather than client for two reasons: the add-in is hand-sideloaded but served centrally out of the caddy image, so a client flag would mean an image rebuild and redeploy to change the behaviour, and `compressible_messages` is a store query the client cannot derive. **The new floor is the only new policy, and it prevents a churn loop the manual button never had:** after a compaction, compressible history drops to ~0 and grows by exactly two rows per turn, so sharing the button's `> 0` threshold would fire on the very next turn against two short messages — a segment the net-benefit guard then declines, i.e. a 10–30s LLM call on a shared Ollama guaranteed to write nothing. Six is three turns of accumulation. **A design defect caught in spec self-review is worth recording:** the obvious effect key — the breakdown object `ContextMeter` renders — fires on *every render*, because `App.tsx` allocated it inline via `withLiveDocument`. The object that changes once per turn is the raw one in `App.tsx` state, so the live-document adjustment moved into the component; the effect keys on the raw breakdown and decides on the adjusted one. `withLiveDocument` narrows `auto_compact` but never widens it: the backend folded the flag and the floor into that field and the client can see neither, so a document edit may relieve pressure but must not arm an unrequested LLM call — a deliberate asymmetry with `can_compact`, which is recomputed from scratch. An automatic run that fails **or is refused** disarms auto (per-session, not persisted — a transient Ollama blip must not kill the feature). Since `5c3ab3d` a **successful run re-arms**; before that the latch had no exit at all and one blip disabled automatic compaction for the rest of the session. The refusal half was a review finding against the original design, which had said a `reason` result "is not a failure and does not disarm": that contradicted the design's own governing principle, since `compact_conversation` generates the segment before it checks net benefit, so a stable refusal condition would have fired an unrequested, guaranteed-fruitless call every turn. A refusal still shows its reason — it just does not fire again unasked, and a manual refusal never disarms. Concurrency was checked rather than assumed: a compaction committing mid-turn can only *duplicate* context, never lose it, because `latest_to_id` is `MAX(to_id)` and the newest segment is by construction inside the injection window — so no locking or send-blocking was introduced. Not in this slice: server-side firing (it would put the measured 10–30s on the attorney's own answer, and `compact_conversation` cannot compute its own target), and the summary viewer, which stays a follow-up. The effect itself has no automated coverage and cannot have any — the add-in has no React test harness, and its assertion scripts can neither render a component nor drive an effect — so the whole behaviour rests on a manual procedure, written up as [testing-compaction.md](testing-compaction.md). **All six checks passed on Word for Mac, 2026-08-26**, and the run is the reason that document exists: six prose turns took history from 11,013 chars (12% of budget) to 1,361 (1%), freeing **9,652** — against **1,598** for an edit-heavy run of the same length, because `history_chars` is measured after `_sanitize_history` and assistant edit blocks are stripped before they count. The floor was observed holding twice at `compressible = 4` (button offered, auto silent), which is check 2 — the churn loop — seen from the quiet side. The failure path rendered `Couldn't condense automatically: the model could not be reached (ConnectError)` once, in red, naming a TRANSPORT failure rather than a verification one, with the manual button still live. **Two defects the sideload found that no assertion could:** the pane offered a Condense button beside a notice saying it had already condensed (`can_compact` is stale-true until the next turn refreshes the counter), and the counter never moved — "the counter updates on your next message" is true and useless when you have just watched something happen on your behalf. Both fixed in `fc21064`; `withReclaimedHistory` adjusts the one line that measurably changed, mirroring `withLiveDocument`. A third followed in `1960b13`: the notice then never expired, because the fix for a notice being wiped within one frame had scoped it to no turn at all. It now carries two breakdown references — `forBreakdown` (the turn it was measured against, scoping the counter adjustment and the button suppression) and `shownFor` (the turn it finished on, scoping display) — which is what gives it exactly one turn either way. **A fourth defect was not in the code at all — it was that the code said nothing.** Auto stayed silent on the VM and there was no way to find out why: `compaction.py` logs only once a run is already underway, so a turn that decided *not* to fire left no evidence anywhere. Three round trips of screenshots and after-the-fact SQL still could not separate the two candidates — a backend withholding the flag, or a pane ignoring it — and the SQL was actively misleading, because a manual Condense in between advances `latest_to_id` and makes `compressible` read 0 regardless of what the turn saw. Every doc-chat turn now logs the decision at INFO under the same `[compaction]` prefix as the run, so one grep gets both: `auto=True` with no `condensed rows` line after it indicts the pane, `auto=False` names the floor that held. The verdict is read back off the breakdown rather than recomputed, so it cannot disagree with what the pane was sent. Measured progression and a symptom→cause table in `docs/testing-compaction.md`. **And the first thing that line found was a fifth defect, in the floor itself.** A grounded MSA turn at a 90,000 budget spends 78,308 chars on fixed content — playbook 30,412 + MSA 24,676 + system 7,212 + document 16,008 — leaving history an allowance of **11,692**. The auto floor was **20,000**: nearly twice what history can ever hold, so it could not arm until roughly 8,300 characters of contract had already been dropped. The floor did not merely fail to prevent truncation, it *guaranteed* it. Captured live: `WARNING chat context 90443 > budget 90000 — truncated document to 15524 chars` on the same turn as `[compaction] auto=False can=True pct=99/90 compressible=4 msgs/12485 chars`, with a Condense button sitting in the pane above a red "part of this document was not sent" notice. The message floor of 6 was the same failure in the other unit — that turn had 4 compressible messages. **Both floors are gone, replaced by one size floor derived from the segment format rather than from an incident**: a segment cannot go below ~940 chars (261-char header, ~20 per quote line, `compaction_min_quotes=4`), so 4,000 is where condensing reliably nets a saving, and that number holds at any budget because it is a property of the format. Replaying the captured turns through the fixed decision arms auto one turn earlier, at `pct=99` with 8,642 compressible chars, and the cut never happens. The regression guard is `test_the_floor_is_reachable_before_the_document_starts_being_cut`, which fails on the old 20,000 AND on a half-fix of 12,000 — it asserts the floor against history's real allowance, not against a remembered number. **The generalisable lesson, third occurrence of the same unit error:** an absolute floor is only safe when its unit is a property of the thing it bounds. `compaction_keep_recent_messages=6` and `compaction_auto_min_messages=6` were counts guarding a size budget; `compaction_auto_min_chars=20000` had the right unit but was calibrated from one afternoon's measurement of the ENVIRONMENT. If a bound's justification cites how big things happened to be rather than how the mechanism works, it will go stale the moment the environment moves. **Two more pane defects surfaced on 2026-09-10, both pre-existing and both a consequence of one automatic failure.** They were individually correct and lethal together. (1) The error notice had no turn scoping — `outcome` carries `forBreakdown`/`shownFor` and expires after one turn, but `error` was a bare string rendered whenever non-null, and `setError(null)` ran only at the head of the NEXT run. An automatic failure also disarms auto, so there is no next run: the red line stayed for the life of the pane. Measured — a failure at 16:04:51 was still on screen at 16:08, across a completed turn that had refreshed the counter around it. (2) `setDisarmed(false)` did not exist anywhere, so one transient Ollama blip disabled automatic compaction for the rest of the session: a failure at 16:04 was still suppressing an armed `auto=True` at 16:33, through a SUCCESSFUL manual condense at 16:10 that proved the model was reachable, a backend restart, and a config change. The only cure was reloading a task pane, which no attorney would know to do. Fixed in `5c3ab3d`: the failure carries `shownFor` (and its own `automatic` flag, rather than reading `busyAuto` at render time, which describes the most recent run and would relabel an automatic failure as a requested one), and a successful run re-arms — settling both disarm causes, since a transport failure means the model is back and a net-benefit refusal means the pool is now worth condensing. Neither is unit-testable (no React harness), so both became manual checks 7 and 8, **both verified by sideload 2026-09-10**. ⚠ The trap that fakes check 8: editing add-in source triggers a Vite HMR remount, and `disarmed` is `useState(false)`, so a hot reload clears the latch exactly as a pane reload would — the first apparent pass was the remount, not the fix. Spec: `docs/superpowers/specs/2026-08-26-automatic-compaction-design.md`. |
| **Compaction gate eval corpus (`gate` kind)** | `feat/eval-compaction-gate` | 15 `gate` cases + `evals/run_gate.py`, sharing `evals/harness.py` with `run_parse.py`; wired into `scripts/eval.sh`. The gate is deterministic and zero-LLM, so it evals exactly like a parser — but the reason to build a corpus rather than more tests is that **the gate is a classifier on a frontier**. Every change to it trades one column against the other: closing a truncation shape costs legitimate quotes, admitting a boundary buys them back at the cost of a fabrication shape. A test suite reports that as "you broke tests." The corpus reports it as what it is — proven by mutation, adding company suffixes to `_NON_TERMINAL_ABBREVIATIONS` prints `[FAIL] gate-accept-sentence-final-company-suffixes` beside `[now-passing] gate-residual-company-suffix-before-a-capitalised-name`, both columns of the trade in one screen. The corpus also takes ownership of the gate's **known defect**, which a test suite structurally cannot hold: a test pins a defect only by asserting the buggy output is *correct*, so fixing it reads as a regression. `test_residual_company_suffix_before_a_capitalised_name_is_not_caught` was deleted and replaced by a baselined case that expects the CORRECT behaviour, so a fix now prints `[now-passing]` and fails the gate until acknowledged. **Building it found a real coverage hole, and the finding is the point:** all 49 tests in `tests/test_compaction_gate.py` passed with `;` and `:` added to `_CLAUSE_END` — a documented, load-bearing fabrication guard with no test that actually exercised it. Three tests looked like they covered it and did not, because a quote stopping just *before* the punctuation is rejected by a different rule (only a full stop may be dropped from a quote's end); the exclusion only bites when the quote **includes** the punctuation, and no test had that shape. Both the corpus cases and a new `test_a_quote_may_not_END_on_a_colon_or_semicolon` now do, and both flip under the mutation. Every case was mutation-verified rather than trusted: forcing `_quotes_a_whole_sentence` to `True` fails all 8 fabrication cases, forcing it to `False` fails all 6 accept cases, so none of the 14 passes for an unrelated reason. `expect.error_contains` is **required** on any rejection case (`check_gate_cases`) — without it a case passes on any error, including one caused by a typo'd row id in the case itself, which is this repo's recurring vacuous-test failure mode. The TS runner learned `gate` as a `BACKEND_ONLY_KINDS` entry so "owned by the other side" and "runs nowhere" stop looking alike to the corpus check. 628 backend tests (was 624), 282 add-in assertions, `parse-py 6/6 · gate-py 14/15 (1 known-failing) · parse 6/6 · match 7/7 · apply 5/5`. |
| **Context counter + attorney-triggered compaction** | `feat/context-compaction` | Every chat turn returns `report.context_breakdown` (measured, post-truncation) → a one-line gauge in the shared pane header, amber past `compaction_warn_pct`. `POST /api/compact` condenses earlier `conversation_store` rows into an append-only `conversation_summary` segment of **extractive verbatim quotes carrying their row ids**, so a deterministic zero-LLM gate can reject any quote that does not match the row it cites. **The gate took five review rounds, and the lesson generalises:** a pure substring check accepted `"accept 12 months"` against `"We will not accept 12 months."` — a fabrication built entirely from real characters. It now requires a **complete sentence**, with `;`/`:` excluded as boundaries (they subordinate a condition where a full stop separates two thoughts) and a narrow title/citation abbreviation list (`Mr. Dr. v. No.`) so `"…Smith v."` cannot pass; company suffixes are deliberately NOT listed because they commonly end a sentence here. One residual is documented and carried as a baselined eval case rather than a test — see the eval-corpus row below for why that distinction matters. A quote that fails is dropped, not fatal — `partition_quotes` keeps every line that verifies and reports the drop count; fatal only when nothing verifies. All-or-nothing was measured to reject ~90% of segments at the model's real ~9% per-quote failure rate, so it made the feature unusable while the per-line guarantee (verbatim AND sentence-aligned) is what actually protects the attorney, and that is unchanged. Injection is summaries-oldest-first below all grounding, then verbatim rows after the highest condensed id. Raw rows are never deleted. **Sizing was reworked after a design challenge that was correct:** `compaction_keep_recent_messages` had been 6 and meant "keep exactly 6" — a message COUNT guarding a character budget, so a conversation holding exactly 6 messages had nothing condensable *while the document was being truncated*. It is now a **floor of 2** (enough that "make that change" still resolves) and everything older is eligible; `compaction_max_quotes=24` became a **ceiling** rather than a size, with the real cap derived from what the caller needs freed — `(raw − needed − header) / 141` — then trimmed oldest-quote-first to the target, stopping at `compaction_min_quotes=4`. Without that, a faithful 20-quote summary of an 8k history freed almost nothing and compaction ran to completion while leaving the document truncated anyway. **Live smoke testing found four defects that the whole test suite could not**, all of them about real data rather than logic: (1) `_norm_shape` collapsed newlines, welding a markdown reply into one enormous "sentence" in which every bullet was unquotable — on its own this rejected every segment, so compaction had never once succeeded against a real conversation; (2) the model substitutes `'` for `"` when quoting text that contains a double quote (it must — the quote line is itself `"`-delimited), and we read that faithful quote as absent from its own row, so all quote forms now fold to one character; (3) backticks and asterisks are dropped from both sides, because the model copies the words and not the emphasis; (4) a segment is not free — a 261-char header plus ~20 chars of `[#id speaker]` per line means that on a short history the summary is structurally **larger** than what it replaces, observed as 1,293 chars condensed into 1,825 (**+532**, compaction making the thing it exists to shrink bigger), now declined outright when `len(content) >= raw_chars`. A fifth was pure arithmetic: the pane computed its reclaim target against the budget rather than the **warn line**, which is 0 everywhere inside the amber band — a Condense button that appeared and did nothing. **Verified in production after the fixes:** rows 161–164, 4 messages → a 551-char segment replacing 2,648 chars, freeing **2,097** of the 6,941 needed, with the pane reporting exactly that and naming the rest as document/playbook/MSA which are never condensed. **Model comparison, scored by the gate itself** (quotes passing / quotes generated is an objective zero-judge metric): production `qwen3.6` via `ChatOllama(reasoning=False)` scored 20/22 in 13.6s; `qwen3-14b` 1/2 in 177s; `qwen3-coder-next` failed to parse. Coder-tuned models are *worse* here — this is faithful prose extraction, not structure generation. `scripts/check.sh` green at 624 backend tests / 282 add-in assertions / tsc clean / eval no regressions. Follow-ups: automatic compaction, re-summarising past ~60 turns, self-calibrating chars/token. |
| **Deterministic eval harness (Tier 1) — zero-LLM regression armor for the matcher and both edit parsers** | `feat/eval-harness` | The self-improving-harness north star needs a "measurement" leg that never existed: every behavior fix this project has shipped was driven by a single trace, with no way to answer "did this make things better overall?" This branch closes that gap for the deterministic half only — **zero LLM calls, runs anywhere, no Docker.** Two genuine coverage gaps closed. **(1) The matcher had NO automated coverage at all.** `clients/word/src/word.test.ts` was 57 lines of pure helpers (`escapeWordWildcards`, `isAmbiguousBlankPlaceholder`, …) and said so in its own header — `findClauseRange`, `searchCandidates`, `tailCandidates`, the 85% completeness guard, `replaceAll`'s snapshot-then-replace, `ambiguousBlankRejection`, `simplifyMultilineReplace`, and the try/finally `changeTrackingMode` restore had never run under a test. 7 `match` + 5 `apply` cases now exercise all of it, against a new Word fake (`clients/word/src/eval/wordFake.ts`) rather than real Word. **(2) Nothing had ever compared the two edit parsers.** `_extract_proposed_edits` (backend) and `extractEditBlocks` (frontend) were each well tested in isolation, but `ChatTab` prefers the backend's list whenever non-empty, so a divergence between them was invisible in every direction that mattered. 6 `parse` cases now run through both. **The fake is written from `CLAUDE.md`'s gotchas and the Office.js docs, deliberately NOT from `word.ts`** — writing it from the caller would encode the caller's assumptions and the eval would confirm itself. Each paragraph stores raw text plus tracked-deletion spans; `reviewedText()` is *derived* (raw minus deletions), never stored, so the two cannot disagree by construction. It refuses (throws) rather than guesses on an edit whose span overlaps an existing tracked deletion — real Word's semantics there are unestablished in anything this fake can check — and throws at construction if a `{raw, reviewed}` fixture pair can't be reconciled by removing one contiguous span. Its eight `body.search` rules (255-char cap, never crosses a paragraph mark, bracket/brace/paren metacharacters misread as wildcards even in literal mode, an escaped-wildcard retry path, `matchWholeWord`, `matchCase`, always the RAW text including tracked deletions, a literal tab never matches) were each proven load-bearing by mutation — delete the rule in a scratch copy, a named assertion in `wordFake.test.ts` flips. Only three of the eight are reachable through `findClauseRange` itself and flip an *eval* case there (rules 4, 6, and 7 — escaped-wildcard retry, `matchWholeWord`, and always-RAW-text): `word.ts` normalizes tabs and newlines out of every query before searching, and every `body.search` call it makes passes `matchCase: false` (never `true`), so the other five rules have no path in from the matcher under eval. `Word.*` enum values are declared `as const satisfies Record<string, \`${Word.Enum}\`>` against the real office-js enums, so a typo or renamed member is a compile error (`TS2820`), not a silent runtime miss. **Headline result: zero genuine `word.ts` gaps.** All 12 matcher/apply cases pass against unmodified shipped code — a real result, not an absence of one, since 11 of the 12 were also proven to *fail* when the guard they name is deleted from a scratch copy. The twelfth, `match-tab-two-column-signature`, names no `word.ts` guard — it is a characterization test pinning a documented limitation (a raw-tab-spanning target genuinely cannot be located this way), not a fix, so there is nothing there for a mutation to flip. The documented gotchas are genuinely fixed in the code. **The two parsers agree on all 6 `parse` cases** — but the case *format* needed a correction mid-build first: the original design defined `expect.edits` as "what either parser extracts before normalization," which is unobservable (`extractEditBlocks` bakes `normalizeProposals` into its own return value, `parseEditBlocks.ts:406`), so comparing raw backend output to already-normalized frontend output was comparing two different pipeline stages, not a divergence. Corrected: `expect.edits` is the backend's raw output (asserted by the Python runner); `expect.normalized` is `extractEditBlocks().blocks`, defaulting to `expect.edits` when extraction is a no-op; and the TS runner additionally asserts `normalizeProposals(expect.edits) === extractEditBlocks().blocks` — the real production invariant, since `ChatTab` normalizes whichever list wins before applying it. **The gate is score-must-not-decrease, never all-pass:** `bash scripts/eval.sh` runs the Python runner (`evals/run_parse.py`) then the TS runner (`clients/word/src/eval/runner.ts`) and prints one score line per kind. A case promoted from a tester's complaint is failing by construction — a binary gate would break the push gate on every promotion and nobody would ever add one. `evals/baseline.json` holds known failures with a reason string, counted (and never hidden) on every run; today it is `{}` — **zero known failures across all 18 cases** (`parse-py 6/6`, `parse 6/6`, `match 7/7`, `apply 5/5`). Both directions of the contract were proven live and reverted: a deliberately corrupted case ([FAIL], `set -e` stops the script before "no regressions", exit 1) and a deliberately baselined-but-passing case (`[now-passing] … — in baseline but passed; remove the entry or check the case`, exit 1). Now wired into `scripts/check.sh` immediately before "all checks passed" (which needed its own fix: the assertions step already `cd`s into `clients/word`, so the gate now captures `REPO_ROOT` once up front rather than re-deriving a path relative to whatever the cwd happens to be). **The only edit to shipped code** is one `export` keyword on `findClauseRange` in `word.ts` — no logic changed anywhere in `word.ts`, `parseEditBlocks.ts`, or `edit_parsing.py`. **Falsifiable by production, not just by itself:** every `edit_failed` `interaction_event` row (`feat/tester-feedback-capture`) is ground truth from real Word — if the fake ever matched where production reported `Couldn't find "…"`, the fake (and the gotcha it encodes) would be wrong; none contradict it yet. +7 backend tests (493 → 500, `tests/test_eval_runner.py`); +37 frontend assertions (221 → 258, `wordFake.test.ts`). Spec: `docs/superpowers/specs/2026-08-19-eval-harness-design.md`; plan: `docs/superpowers/plans/2026-08-20-eval-harness.md`. |
| **Application logging actually reaches the log (and the VM smoke that found it)** | `fix/production-logging-and-vm-docs` | **Every `logger.info` in `api/`, `graph/`, `memory/` and `skills/` was discarded in production, and had been since the project began running under uvicorn.** Uvicorn's `LOGGING_CONFIG` configures only `uvicorn`, `uvicorn.error` and `uvicorn.access`; it never touches the **root** logger, which therefore keeps its default WARNING level and *no handler at all*. App records propagate up to it, find nothing that will handle them, and die — silently below WARNING, since `logging.lastResort` covers only WARNING and above. That is why `AUDIT WRITE FAILED` (ERROR) has always been visible while nothing at INFO ever was. **The casualty is the in-flight turn logging** (`feat/in-flight-turn-logging`), built specifically so that on this VM a slow LLM could be told apart from a wedged one: `[llm_caller] -> ollama …`, `<- ollama … in 62.3s`, `[legal_research] -> doc_chat …` are all `logger.info` and none of them has ever appeared in `docker compose logs backend`. The feature was shipped, tested, documented — and inert. **Why the tests passed:** `tests/test_inflight_logging.py` asserts through `caplog.at_level(logging.INFO)`. pytest's logging plugin installs its own root handler and that call forces the level on top of it, so those assertions hold whether or not the application configures logging — a green suite asserting on a handler the *harness* supplied. This is the vacuous-test failure mode this repo already warns about for module-boundary patching, in a new place. **Fix:** `api/main.py::configure_logging()`, called at import (uvicorn configures its own logging *before* importing the app, so import time is late enough to see an unconfigured root and early enough to catch startup records). `basicConfig` is deliberately called **without** `force=True`: it no-ops when root already has handlers, which keeps it from tearing out pytest's capture under every other test, and uvicorn's own loggers set `propagate=False` so nothing is double-printed. Level comes from a new `log_level` setting (default `INFO`) — a **declared** field, not an `os.environ` read, because pydantic-settings forbids extra `.env` keys, so an undeclared `LOG_LEVEL=…` in `.env` would crash the backend on startup with `Extra inputs are not permitted`. **Tests run in a subprocess** (`tests/test_logging_config.py`, +4) — the only way to reproduce what uvicorn sees, since pytest's own handler masks the bug in-process. One test drives the real uvicorn `Config.configure_logging()` first and then imports the app, asserting our records survive *and* that uvicorn's own appear exactly once. Three mutations verified: dropping the call fails 3 of 4, defaulting to DEBUG fails only the level-boundary test, and swapping the settings field for an `os.environ` read fails only the declared-field test. **Found by the first full VM verification pass (2026-08-19, SRV-AGENT-01).** That pass also proved the feedback loop end to end on the real deployment: 5 tables with the `request` column present, both endpoints failing in their intended directions, a real Word session producing 2 feedback rows (one card ⚑ with a live `trace:`, one header miss report grouped by `doc:`) and `edits_proposed` total=2 reconciling exactly against `edit_applied` 1 + `edit_discarded` 1 with **zero `edit_failed`** — the matcher found its target both times. The served bundle carried all three probe strings, confirming the Docker-built pane has actually ended the backend/frontend drift it was built to end. Docs corrected from what the run exposed: `docs/feedback-loop.md` (the report needs `$DC run --rm --no-deps backend python -m …` — there is no `uv` in the image and `app-db` publishes only `127.0.0.1:5434`; the health-check curls needed the Caddy form since the backend publishes no host port; and a ⚠️ that **`finding_commented` means the add-in inserted a Word comment, not that an attorney commented on a finding** — a naming trap that made a correct zero look like a dropped write) and `docs/deploy-vm.md` (a `DC` shorthand, pane- and logging-verification steps in Step 5, and the `bad marshal data` entry below). |
| **Postgres outage degrades instead of killing the turn** | `fix/postgres-degradation` | Redis had a deliberate degrade path (`_get_stateless_graph`, amber banner); Postgres had none, so `docker compose stop app-db` made **every** turn — review and chat alike — stall and die in the pane with a raw `couldn't get a connection after 30.00 sec`. On the VM that is the difference between "memory is flaky today" and "the tool is broken". Two parts. **(1) Bound the pool:** `memory/db.py` never passed `timeout=`, inheriting psycopg_pool's 30s default; it is now `config.db_pool_timeout` (3.0s) plus an integer `connect_timeout` floored at 2 for blackholed hosts. This is the leveraged half — one change fixes every Postgres caller at once, *including the two recall reads that already degraded correctly but only after burning 30s each first*. **(2) Wrap the audit write:** `write_audit_log` was the single unwrapped store call on the turn path; it now logs the full entry at ERROR and flags `memory_degraded`. **The follow-up's own diagnosis was wrong** and is corrected in its row: it prescribed wrapping the recall reads, which were already wrapped. `save_review` stays loud, `append_turn` stays best-effort, `init_db()` stays fatal at boot. Constraint 6 reworded to state the relaxation explicitly rather than let code and doc disagree. 11 new tests, each mutation-verified; the seam test drives the real node into the real payload builder because `memory_writer` runs *after* `output_formatter` and a `state[...]` flag there would pass every node test while never reaching the banner. **Live smoke 2026-08-18 (Word for Mac, `app-db` stopped mid-session) — all four states pass:** (1) chat answered normally + amber "Memory unavailable this turn" banner; (2) the full review rendered — gate verdict, 4 blockers, findings — alongside `This review could not be saved (couldn't get a connection after 3.00 sec)`, i.e. the new bound visible in the pane text and *both* app-db writes failing without either hiding the other; (3) `AUDIT WRITE FAILED` logged the complete entry (session, attorney, skill, request, name) for both a `research` and a `contract_review` turn, each returning 200; (4) `docker compose start app-db` → healthy again with **no backend restart** (the pool self-heals). The log also caught a nuance worth keeping: the recall reads failed via `terminating connection due to administrator command` — the fast broken-connection path — not by waiting out the timeout, confirming that observed latency during an outage depends on whether the pool holds stale connections or must open a new one. |
| **Tester feedback capture (flag + read-back, no UI report)** | `feat/tester-feedback-capture` | A session_id spans every turn in both Word tabs — a dozen review/chat prompts can share one — so feedback keyed to it could only ever point at a whole pane session, not a turn. `POST /api/query` and `/resume` now mint a `turn_id` (uuid4, per request) and return it alongside the real 32-hex OTel `trace_id` (`observability/spans.py::current_trace_id` — best-effort, `""` with tracing off or no valid span, never raises). **`state["trace_id"]` had quietly held the session_id and was read nowhere** — repointed at the real per-request trace id instead of adding a second field alongside a wrong one (`test_state_trace_id_is_no_longer_the_session_id` pins it). Two new Postgres tables (`memory/feedback_store.py`; `memory/db.py::init_db()` now creates five) carry the opposite failure policies review/conversation already established: `feedback` (an attorney's written report + a replayable input snapshot) writes LOUD — no try/except, because the attorney was already shown "Sent ✓"; `interaction_event` (every Apply/Discard/failure/jump-not-found against our own suggestions) writes QUIET — `record_event`/`record_events` swallow and log, since telemetry must never be the reason an Apply fails. Two tables rather than one `kind` column, by design: ~100:1 expected volume, a `snapshot` column that would sit NULL on nearly every interaction row, GROUP-BY-shaped reads on one and prose reads on the other, and different retention (events are prunable, feedback isn't). `POST /api/feedback` / `POST /api/events` (`api/routes/feedback.py`) both resolve identity through `resolve_user_id`/`resolve_user_name` — never the request body — so the dormant O365 SSO cutover reaches feedback for free and a client can't spoof an attorney id; they fail asymmetrically on purpose, `/api/feedback` 403s when disabled and 500s on a store failure, `/api/events` always 200s (a lower `recorded` count is how a disabled flag or a store failure show up, since a telemetry outage must be invisible in the pane). Snapshot is capped **per-field** (`truncate_snapshot`, `feedback_snapshot_max_chars` default 200 000, truncation-marked) so one large field can't starve another. `FeedbackPanel.tsx` renders **in-pane**, deliberately not `displayDialogAsync` — a dialog is a separate window/origin and the Mac webview is already on record elsewhere in this file as unreliable for that class of thing. Two entry points feed it through one module-singleton channel (`requestFlag`/`onFlagRequested` in `feedback.ts`): a ⚑ on every finding/edit card, and one header **Send feedback** button with no card underneath it — the one that matters most, since *"it never flagged the assignment clause"* is a miss with nothing to hang a flag off, and a miss is the one failure class invisible to both the developer and any eval harness. A standing header line — *"During testing, your use of the assistant's suggestions is recorded so we can measure what it gets wrong"* — plus the panel's own per-send line naming exactly what it attaches, makes the recording disclosed rather than merely not-hidden. Review caught the header path bypassing the document-attach effect (it called `setFlagTarget` directly, so its own disclosure was false for the entry point it exists for) and a missing panel `key` that let flagging a second card reuse the first panel's mounted instance — together with an in-flight `sendFeedback` outliving an unmount, either could ship typed-but-unsent text against the wrong target or silently close a panel out from under whatever the attorney had started typing; all three fixed before this landed. Every suggestion surface now calls the same never-throwing `recordEvent` (`void`, not a `Promise` — a caller can't `await` a telemetry call by accident): `findings_rendered`/`edits_proposed`/`preferences_suggested` carry a count in `detail`; `edit_applied`/`edit_discarded`/`edit_failed`/`redline_applied`/`redline_failed` (failures carry the error string) exercise matcher machinery (`findClauseRange`, the 85% guard, wildcard escaping, tab-segment reduction, …) that until now had only anecdote behind it — deliberately including a short quoted excerpt of the targeted clause (`target_ref`, capped at 200 chars) and, on `edit_failed`, the matcher's own `Couldn't find "…" in the document.` message in `detail` (capped at 500 chars); the design spec originally said the event log holds "ids and counters only," which undersold this, and the spec text was corrected rather than the excerpt walked back, since the excerpt is what turns a bare failure tally into a measurement of which clause the matcher missed. `edit_jump_notfound`/`finding_jump_notfound`/`finding_commented`/`preference_added` round out the set. `scripts/feedback_report.py` is the entire reporting story, by design — *"feedback nobody reads is worse than none."* No dashboard: `uv run python -m scripts.feedback_report` calls `init_db()` (idempotent, safe against a database that has never seen these two tables) and prints the last 50 feedback rows plus a `surface`/`action` count table from `event_counts()` — the denominators several roadmap rows below have been waiting on. **Iteration 2 (2026-08-17), driven entirely by the first live run:** `interaction_event` gained a `request` column (added by `ALTER TABLE … IF NOT EXISTS`, since `CREATE TABLE IF NOT EXISTS` will not add a column to an existing table — same pattern as `audit_log.user_name`) carrying the attorney's own question on the two chat counters, because without it the **spurious-edit rate** — the roadmap's clearest open rate question — needed a manual trace lookup per turn and was not computable in SQL at all; `edit_proposal_turns()` puts question, proposal count and the attorney's applied/discarded/failed verdict on one row, so a factual question with `proposed > 0` is visible by scanning. All three read functions gained optional `since`/`document_id`/`attorney_id` filters through a shared `_filters()` helper (`timestamp` is TEXT, but every writer stores `datetime.now(timezone.utc).isoformat()`, so one format and one offset make a lexicographic `>=` a correct chronological comparison), surfaced as `--days`/`--since`/`--attorney`/`--document`/`--limit`; unfiltered the store is cumulative for all time, so pilot week one and week ten looked identical. The header ⚑ path also now resolves a `documentId` in the shared flag handler rather than inheriting an empty one from `EMPTY_TURN`, so a miss report can finally be grouped with other feedback on the same contract — the live run had produced exactly one such orphan row. **Live-verified 2026-08-17 (Word for Mac sideload):** 3 feedback rows and 17 events across two pane sessions on one unsaved NDA; `turn_id` in the feedback row matched `turn_id` in the Langfuse trace metadata **byte-for-byte in both directions**, and one `turn_id` reconstructed a whole episode (2 edits proposed → 1 flagged *"i didnt ask this"* → 1 applied, 1 discarded) joinable straight to the prompt that produced it. Both review-loop fixes were confirmed by real data: the flagged finding was `hasQuotedText: false` (the class that had **no** ⚑ at all before the Task 6 fix) and the header/miss row carried 12 920 chars of document text (its snapshot would have been `{}` before the Task 7 fix). +38 backend tests (468 → 478), +8 frontend assertions (199/199). **Word smoke completed 2026-08-18.** A forced matcher miss (edit the target text in the document, then Apply) produced the first-ever `edit_failed` row carrying the matcher's own words — `Couldn't find the exact target text in the document (looked for "Governing Law. This NDA shall be governed by, and …")` — which is the field measurement `findClauseRange`/`searchCandidates`/the 85% guard have never had; the card also surfaced a clear red error rather than failing silently. `request` confirmed live: a `who signs?` turn recorded `proposed=0` (the spurious-edit bug did **not** fire that run — now a countable observation instead of a remembered one). Header **Send feedback** renders compact, not as a full-width bar (`align-self: flex-start` on `.feedback-open`), and the ⚑ sits cleanly beside Discard and on assistant messages. Backend halves proven by curl against the live stack: `edit_failed`'s error string round-trips verbatim (apostrophe intact), and with `app-db` **stopped** `/api/events` returns `200 {"recorded": 0}` in **2.3 ms** while `/api/feedback` returns **500** and writes nothing — the quiet/quiet-vs-loud split holding under a real outage, not just under an injected mock. **The smoke also surfaced a pre-existing High bug unrelated to this branch** — with `app-db` down a doc-chat turn stalls 30 s on `psycopg_pool` and dies, because Postgres has no equivalent of the Redis degrade path; logged as its own follow-up rather than fixed here (it lives in `graph/`+`skills/`, which this branch is constrained never to touch). Not visually confirmed: an Apply with `app-db` stopped — Apply is pure Office.js and never calls the backend, and the telemetry POST behind it was proven above. **How to run and read it:** [docs/feedback-loop.md](feedback-loop.md) (the counter-vs-per-item trap, the feedback→turn→trace join, the rates worth watching, and what it still cannot answer); the tester-facing half is [docs/using-the-assistant.md](using-the-assistant.md), "Telling us what it got wrong" (split out of `tester-setup.md`, which is install steps only). |
| **In-flight turn logging (a slow LLM no longer looks like a hang)** | `feat/in-flight-turn-logging` | On SRV-AGENT-01 (2026-08-14) a chat turn sat on "Thinking…" and `docker compose logs backend` showed **five completed `200 OK`s and nothing else** — so it read as though the request never arrived. It had: **uvicorn writes its access line only when the response completes**, and every log line on both LLM paths fired post-call, so a turn that was merely slow left no trace anywhere. "Queued behind compliance-bot on the shared Ollama at `172.20.0.22`" and "wedged" were indistinguishable. Both paths now announce BEFORE the call — `[llm_caller] -> ollama model=… task=… messages=N chars=N url=…` and `[legal_research] -> doc_chat model=… doc=N chars grounded=bool history=N msgs=N` — and report elapsed on the way back (`<- ollama 4578 chars in 62.3s`), including on failure (`FAILED after 200.1s`), since silence after a timeout reads as a hang too. The `grounded=` / `doc=` fields also make a lean turn distinguishable from a grounded one at a glance, which previously needed a trace. **The same incident had a second, deeper cause: generation was completely unbounded.** `num_predict` appeared NOWHERE in the codebase, so Ollama's default applies — generate until the context window fills. With an ~11k-token prompt against `num_ctx=32768` the model had ~21k tokens of room, and used them: asked to *"fill the title"* against a title that was **already** `Chief Growth Officer`, and *"fill legale name"* against a name already `Sony`, it could not resolve filling something already filled and looped one self-doubt paragraph hundreds of times **inside a JSON `rationale` field** until it ran out of room. The turn was not queued; it was generating. New `ollama_num_predict_chat` (2048 — chat replies observed at 13–373 chars) and `ollama_num_predict_review` (8192 — a real review emits ~4.6k tokens of tables), applied to **all three** call sites: `_build_llm`, `_build_json_llm` (the likelier looper, since it fires precisely when the first reply was already malformed) and `llm_caller`. A degenerate loop now truncates instead of stalling. Note the history sanitisation shipped earlier the same day contains the blast radius: the monster ```json``` block is stripped on replay, so it cannot poison later prompts. **The load-bearing property is ORDERING, not the text** — a start line emitted after the call would satisfy a naive "is it in the log" assertion and fix nothing — so the tests read `caplog` from INSIDE the mocked call, and both mutations (moving either announce after its call) fail them. +5 tests (439), each mutation-proven — removing either cap fails its test. ⚠️ backend change — `start.sh` restart / VM redeploy required. |
| **Fill-vs-overwrite guard + standalone preference wording** | `fix/fill-overwrite-guard-and-preference-wording` | Two defects from the **VM** smoke on 2026-08-14, both found by reading the assembled prompt rather than the pane. **(1) SCOPE violation that destroys data.** With the signature block reading `Signed by: Jane Doe` and two genuine blanks still empty (`Title: [__]`, `for and on behalf of [__]`), *"fill blanc singed by with suzy qutreo"* produced `Signed by: Jane Doe` → `Signed by: Suzy Qutreo`: the blanks were skipped and a real value was overwritten as a one-click tracked change. `CHAT_SYSTEM_PROMPT`'s SCOPE rule forbids exactly this ("'Fill' means putting a value into an EMPTY placeholder — it never means replacing text that is already filled in") and the model ignored it — its own rationale said "**Updates** the counterparty signatory name", so it knew. New `fillOverwriteWarning` / `annotateFillOverwrites` in [clients/word/src/parseEditBlocks.ts](clients/word/src/parseEditBlocks.ts) read the same two signals deterministically: the verb the attorney used (`fill|complete|populate|blank`) and whether `target_text` actually contains a placeholder (`[...]`, `___`, `...`). **Advisory, not a refusal** — "fill in the governing law with Tennessee" on a clause already naming Delaware is legitimate — so it renders as a ⚠ badge on the card with Apply still enabled. Wired in **ChatTab at the point of use**, beside `normalizeProposals`, because the backend's `proposed_edits` win whenever present and it additionally needs `question`, the attorney's own words, which only that scope has. **(2) Preference wording.** "remeber that our client is Sony" yielded the preference line *"remember that our client is Sony"* — the request echoed verbatim — which was approved and now rides in **every** prompt on **every** document as an imperative addressed to nobody. `CHAT_SYSTEM_PROMPT` now requires a STANDALONE statement ("Our client is Acme Corp."), never a restatement of the request, with pronouns resolved; `_PREFERENCE_RETRY_SYSTEM` already said this but only applies when the retry fires, and here the model emitted the block first try. **Local smoke (2026-08-14) — guard confirmed both ways:** "fill blank signed by with Suzy Quatro" against a filled `Signed by: Jane Doe` produced the ⚠ badge with Apply still enabled; the control "fill the title with Chief Growth Officer" against a genuine `Title: [__]` produced **no** badge and applied cleanly (the model's own rationale reading "Fills the blank title field"). So it discriminates rather than warning on everything. **The prompt half-fixed the preference wording, so a deterministic strip was added.** Told to write standalone, the model resolved the pronoun ("our" → "the") but kept the imperative: "Remember that the client is Sony." New `_normalize_preference_line` peels off a leading `remember|keep in mind|bear in mind|note|make a note` wrapper (optionally `that`/`to`) and re-capitalises, applied inside `_extract_proposed_preferences` so it covers both the first reply and the retry. **Content-agnostic on purpose** — a preference is not always a fact about the client, so the wrapper comes off and the substance is untouched: "Remember I always want indemnity flagged red." → "I always want indemnity flagged red.", while "Always flag uncapped indemnity." is left alone. `note` needs a word boundary so "Notes must be short." is safe; a line that is nothing but the wrapper is kept as-is. **The rule pile was then replaced with one signal, on review feedback.** The fix below is what the branch shipped, but the way it got there was a rule per failing trace — `remember` needed `always`, which needed `never`, which needed an interrogative test to undo its collateral. That is an open set (English-only, defeated by a typo — "remeber" only worked because the MODEL understood it), and the next phrasing would have needed another entry. **The signal was in the wrong place.** In every trace examined the model announced what it did — *"I have remembered that you want me to always flag uncapped indemnity in future reviews"* — including `004bccfe`, where the user-side rules missed. So `_MEMORY_PROMISE_RE` / `_looks_like_memory_promise` now detects the MODEL's own claim, the exact preference-side twin of the existing `_EDIT_PROMISE_RE`. `_ALWAYS_NEVER_RE`, `_INTERROGATIVE_RE` and `_is_question` are **deleted** — they existed only to patch each other. `_should_retry_preference(request, reply)` is now an OR of two INDEPENDENT signals (attorney's explicit verb, model's narration), justified by cost asymmetry: a miss loses a preference while the model claims it saved it (invisible), a false positive costs one ~200-token call that returns an empty block for a one-off (self-correcting). Context-only requests short-circuit it, reusing guard A's own condition rather than adding a rule. **This is what stops the list growing** — new phrasings are absorbed by the narration detector, so `_PREFERENCE_REQUEST_RE` is frozen at its current size. **A live trace had already falsified one of this branch's assumptions.** `_PREFERENCE_REQUEST_RE` deliberately excluded bare `always`/`never`, justified by a test saying "'always flag X' is the prompt's own worked example and the model emits a block for it reliably, so the net is not needed". Trace `004bccfe`: **"Always flag uncapped indemnity"** — that exact example phrase — produced *"I have remembered that you want me to always flag uncapped indemnity in future reviews"* with **no block and no retry**. The preference was silently lost while the model claimed it was stored, which is the worst possible failure shape. The original blocker ("does this clause always apply?") was a **question**, so the real discriminator was sentence FORM, not the verb: new `_should_retry_preference` = the narrow predicate OR (always/never AND not interrogative). Kept as a **separate** predicate — widening `_looks_like_preference_request` itself would let "keep in mind we always use Tennessee law" produce a card, inverting what "keep in mind" means. +8 backend tests, +13 frontend assertions (434 / 191). **Ten mutations run.** Nine detected immediately; the tenth exposed a VACUOUS TEST — reverting the retry gate to the narrow predicate left the whole suite green, because the new tests only exercised the predicate in isolation and never drove it through `_run_doc_chat`. Fixed with two end-to-end tests. Third time this session mutation caught coverage that a green suite hid. The detected set: guard disabled, placeholder check removed, fill-verb check removed, prompt line stripped, normalization unwired, strip widened to `always|never` (immediately breaks "Always flag uncapped indemnity."), wrapper-only fallback removed. Note the assert-count gate in `check.sh` caught the count change unprompted — working as designed. ⚠️ backend **and** frontend change — `start.sh` restart plus a redeploy/rebuild. |
| **Word pane built in Docker (ends silent prod/backend version drift)** | `fix/docker-pane-build` | The VM was serving a task-pane bundle built **2026-08-11** while its backend had been redeployed twice since — and nothing could ever have corrected it: `clients/word/dist/` is gitignored so `git pull` never updates it, `docker compose up --build` rebuilds only the backend image, and `SRV-AGENT-01` has no `npm` (`npm ci` there fails with *Command 'npm' not found*). Confirmed by probing the deployed bundle: `"would put the same text in every field"` present (the replace_all guard) but `"usually the wrong field"` absent (the same-day bare-blank `replace` guard), with `dist/taskpane.html` dated 11 Aug and owned by `root` — so it had been produced by a container or copied in by hand once, then frozen. **Production was running an unknown frontend version, silently.** New [clients/word/Dockerfile](clients/word/Dockerfile): a `node:20-alpine` stage runs `npm ci && npm run build`, a `caddy:2` stage takes `/app/dist` → `/srv`; `docker-compose.remote.yml`'s `caddy` service swaps `image: caddy:2` + the `./clients/word/dist:/srv:ro` mount for `build: ./clients/word`. Backend and pane now come from one commit by construction and the host needs no Node. The Caddyfile stays a bind mount so cert/hostname work (runbook Step 2) needs no image rebuild; a `.dockerignore` keeps `node_modules/` and any stale host `dist/` out of the context. This works because `vite.config.ts` already loads `office-addin-dev-certs` **only** when `command === "serve"` — a guard added earlier precisely so a clean container with no sudo/cert tooling could build. **Verified end to end:** the image builds (`docker build ./clients/word`); `/srv` inside it holds `taskpane.html`, the icons and `assets/taskpane-BpTCZ68P.js`; that bundle **contains `"usually the wrong field"`** — the same-day guard the VM's 11-Aug `taskpane-BWQOaDEw.js` lacks, which is the whole point; the runtime image has **no node/npm** (multi-stage discarded the builder) at 84.9 MB; and running it with the real Caddyfile serves `GET /taskpane.html` → **200** (490 B) and `GET /assets/taskpane-BpTCZ68P.js` → **200** (186 640 B) over HTTPS. `npm run build` is `tsc --noEmit && vite build`, so a type error fails the image rather than shipping a broken pane; the only packages with install scripts are `esbuild` and `fsevents` (the latter macOS-only, skipped on alpine). **Environment note that cost an hour:** the build initially failed on every Docker Hub pull with `EOF` — including `hello-world` — while `curl` to `registry-1.docker.io` returned a normal 401. `--check` finally named it: `proxyconnect tcp: dial tcp 192.168.65.1:3128: i/o timeout`, i.e. Docker Desktop's own internal proxy was unreachable. **Cause was the corporate VPN**; disconnecting it fixed the pull instantly. Note the conflict this creates — the VPN is required to reach `SRV-AGENT-01` but breaks `docker pull`, so pull base images with the VPN down, then reconnect to deploy. ⚠️ Still worth running `docker pull node:20-alpine` on the deploy host first: it is the one new base image, and the VM's earlier backend build resolved `python:3.12-slim` from cache, so its Hub reachability is unproven. Runbook Step 3 updated: nothing to run by hand any more. |
| **Stored output no longer primes the model (history sanitisation + preference guard)** | `fix/history-priming-and-preference-guard` | **The bug that explains why a shipped prompt fix stopped working.** On the VM, "keep in mind that our client is blizzard" produced an Add-preference card that the corrected prompt explicitly forbids. Ruled out in order, with evidence: the prompt was live (`exec backend` printed **6320** + `True`), the model was byte-identical to local (digest `07d35212…`, 36.0B, Q4_K_M, `qwen35moe`), and `temperature=0.0`. A clean-history local repro at temp 0 then handled **all three** phrasings correctly. The difference was the assembled history: VM conversation `2ae99ecc` held **three prior turns that each emitted a preference block** for exactly this phrasing — turns that were *correct when written*, under the pre-correction prompt. **Root cause is a design seam, not a typo.** An assistant reply fuses two channels into one string — conversation (prose) and machinery (fenced ```json``` / ```preference``` blocks). Every consumer separates them (`_extract_proposed_edits` / `_extract_proposed_preferences` on the backend, `cleanedProse` on the frontend) **except the path that replays the reply into the next prompt**. To a model, history is not a log, it IS prompt, and in-context demonstrations outrank system instructions — so the store silently turned past output into few-shot examples nobody wrote. ⇒ **Prompt fixes are not retroactive while the store replays raw output.** **Fix B (root):** `_sanitize_history` strips fenced blocks from replayed ASSISTANT turns, applied at the **point of use** in `_run_doc_chat` — not inside either loader, since the durable store and the Redis fallback both replay raw output and a transform in one is bypassed when the other wins (same lesson as `normalizeProposals` in ChatTab). Stripping happens **on read, not on write**: the store stays a faithful record and already-contaminated rows become harmless with no migration. The value-carrying prose survives ("I have updated the signatory name to John Doe"), so "the legal name we filled recently" still resolves — the reason `chat_history` can't simply be scoped out. **Fix A (defence in depth):** `_looks_like_context_only_request` drops an unsolicited preference block based on the **attorney's own words**, so it is immune to model, context length, and history state. Deliberately a NEGATIVE list — requiring a positive match would suppress "always flag uncapped indemnity", unmatchable without also catching "does this clause always apply?". An explicit storage request in the same message wins. **Scoping was never the lever:** `conversation_store` is correctly keyed `(document_id, attorney_id)`, reviews by `document_id`, prefs by `attorney_id`. Keys bound blast radius, not content — a perfectly-scoped store still poisons its own scope. Narrowing to per-session would delete slice 2's whole point and still not help (3 turns primes it). ⚠️ **This inverts the priority on the unsaved-document follow-up below:** that bug mints a fresh `document_id` per pane reload, which was accidentally handing every local test clean history and masking this. Ship sanitisation FIRST — fixing identity alone would make priming more frequent, not less. +11 tests built from the verbatim VM rows. **Four mutations, all detected** — and mutation earned its keep again: `test_explicit_storage_wins_when_both_phrasings_appear` initially passed **vacuously**, because a single fixed stub reply let the preference-retry re-emit the very block the guard had just suppressed. Fixed by giving the retry a distinct block-free reply. Also hardens **`scripts/start.sh`**, which turned this session's port conflict into a 20-minute hunt: the health check was a bare `curl -s` with **no `-m`**, so against a listener that accepts and never answers it blocked forever — the script stopped silently after uvicorn's banner, never printed "Backend ready.", never started Chainlit, and looked exactly like a crashed backend. Now a bounded retry (12 × 2s) with a liveness check that distinguishes "still booting" from "already dead", plus a **pre-flight port check that refuses to start** when anything already holds `:8000` — nothing of ours is running at that point, so any listener there is someone else's (a stale run, or VS Code's auto port-forwarding, which keeps the socket bound after its target dies). Both paths were tested: hijacked port fails in **1s** with the owning PID, clean port reaches "Backend ready." and a live `200`. Also dropped `docker compose ps | head -7`, which truncated the service list and made redis look absent while it was up and healthy. ⚠️ backend change — `start.sh` restart required. |
| **Edit-block guards + preference capture** | `fix/edit-block-guards-and-preference-capture` | Three defects found by reading trace `cc81804f` — the same turn that cleared the fabrication check also emitted `[{replace [Legal Name] → Blizzard Corp}, {replace [__] → Blizzard Corp}, {replace Title: [__] → Title: [__]}]`. **(1) Bare-blank `replace` was unguarded.** `isAmbiguousBlankPlaceholder` was enforced only inside `replaceAll` — a plain `replace` on `[__]` fell through to `searchFirst`, which takes the first match. Verified against the actual document: the first `[__]` sits **12 846 chars in**, at `Signed by: [__]` in the execution block, opposite `Signed by: John Doe` — so the edit would have written a **company name into a signatory-name field** as a one-click tracked change. Same root cause as the replace_all guard (the target names no field), different damage, one occurrence at a time. Unified into a shared `ambiguousBlankRejection(action, target)` in [clients/word/src/word.ts](clients/word/src/word.ts) with an action-specific message; both call sites now use it. **(2) No-op edits were applyable cards.** `Title: [__]` → `Title: [__]` renders as a card the attorney must reason about and dismiss, and applies a tracked change that changes nothing. New `isNoOpEdit` filter in `normalizeProposals` ([clients/word/src/parseEditBlocks.ts](clients/word/src/parseEditBlocks.ts)), applied **last** — splitting and duplicate-collapsing can each surface a no-op invisible in the original proposal. Third sighting of no-op cards, first with a deterministic repro. **(3) Preference requests were silently dropped.** "remember that uor client is blizzard corp" produced "I have noted that your client is Blizzard Corp **for this conversation**" and no ` ```preference ` block — the attorney wanted it in `USER.md`. Two-part fix mirroring the edit-promise → `format='json'` retry: the prompt's REMEMBERING PREFERENCES rule now states that standing **facts** about the engagement (client, counterparty, matter) are preferences and not merely conversational context; and a new `_looks_like_preference_request` + one `doc_chat_preference_retry` call fires only when the user used explicit memory phrasing AND no block came back. The retry deliberately **omits the document** (it needs only request + reply, so it stays fast) and `_run_doc_chat` now returns `(content, edits, prefs)`. **Storage, not usefulness, is the line (corrected 2026-08-13 from live feedback).** The first cut treated "keep in mind…" as a preference request, and live it produced an unwanted Add-preference card for "keep in mind that our client is EA games" (trace `4e3a2d94`) — the attorney wants a card only when they asked us to **store** something. "Remember…", "from now on…", "for future reference…" ask for persistence; "keep in mind…", "bear in mind…", "note that…", "for this document…" ask only that we use it in the current conversation, which `chat_history` already does. Both the prompt and the detector now draw that line, and a test asserts they draw it in the same place — **the prompt is the one that decides** (the unwanted card came from the prompt with no retry involved; the detector only nets a forgotten block). **The detector is also narrower than first written, and a test forced that:** the initial regex matched `always (flag|use|apply|…)`, which fires on `"does this clause always apply?"` — a false positive costs a real LLM round-trip on the commonest kind of turn. Bare always/never were dropped and the gap is documented by a test, since instruction-shaped preferences are the prompt's own worked examples and the model emits blocks for them reliably. +9 backend tests and +13 frontend assertions (415 / 178). **Six mutations, all detected:** guard skipping `replace`, no-op filter removed, retry removed, retry made unconditional, detector re-admitting "keep in mind", prompt losing the storage/context distinction. ⚠️ backend **and** frontend change — `start.sh` restart plus a Word reload; the two guards are apply-path code and want a sideload smoke. |
| **MSA-truncation + unsourced-value prompt guards** | `fix/truncation-fabrication-docid` | Two structural, model-neutral prompt corrections for the smoke's remaining model-behavior findings. **(1) MSA truncation.** `_MSA_COMPARISON_DIRECTIVE` ([skills/contract_review/contract_review.py](skills/contract_review/contract_review.py)) gains a rule 4: the MSA may be an EXCERPT, a truncation boundary is an artifact of this system and NEVER a defect in the MSA, and anything past the cut falls under rule 3 (treat the MSA as silent). **Applied to BOTH paths.** The first cut put the rule only in the review directive, but the observed false finding (trace `85533de4`) was a `query:research` **doc_chat** turn, which never sees `_MSA_COMPARISON_DIRECTIVE` — it builds MSA grounding from `_CHAT_MSA_NOTE`. Trace `6053bba2` proved the gap live: `MSA truncated to` present in the assembled prompt, `EXCERPT` / `never a defect` absent. `_CHAT_MSA_NOTE` now carries the same disclaimer. Both paths share `grounding.attach_parent_msa`, so both had the exposure. This is option (c) from the follow-up — **the cap is deliberately left at 24 000** per decision, since truncation is currently a load-management choice, not a bug. `grounding.attach_parent_msa` already wrote a trailing marker; nothing told the model what to do with it. **(2) Unsourced values.** A SOURCES paragraph in `CHAT_SYSTEM_PROMPT` + `_JSON_RETRY_SYSTEM` ([skills/legal_research/prompts.py](skills/legal_research/prompts.py)): every `new_text` value must come from the document, the playbook/MSA, or the user's own words; never supply an address, registered office, full legal entity name, registration number, or date that is absent from those sources; **naming a party tells you the name and nothing else**; if a value was not given, leave the placeholder and ask. **Placement is the substantive part** — a general *"Do not invent facts"* rule already existed in the conversational RULES block and did **not** prevent the fabricated Sony address, so the constraint is restated where the edit path reads it and names the specific fact classes. **This is a mitigation, not the fix.** A prompt rule lowers frequency; it cannot bound the worst case, and the failure hands an attorney a one-click Apply into a legal instrument. The code-side provenance guard stays open as a High follow-up. +2 tests (405 total), all three additions **independently mutation-proven** (stripping either prompt's paragraph, or rule 4, fails its test alone). **VERIFIED LIVE 2026-08-13 (traces `24dea1eb` / `7c366aaf` / `cc81804f`).** Re-ran the fabrication scenario with Blizzard in place of Sony. Prompt confirmed live first — the trace's system message is **5598 chars, an exact match for the new `CHAT_SYSTEM_PROMPT`** against 5022 pre-change — so this tests the rule, not a stale backend. Given every primer the Sony run had (a named well-known company, `[Address]` adjacent to `[Legal Name]` in the preamble, and its own offer to "fill those in with Blizzard Corp's information"), the model wrote back **exactly the four words supplied**: `[Legal Name]` → `Blizzard Corp`, with `[Address]` present once in the document and never targeted. No invented address, no "Blizzard Entertainment, Inc.", no Irvine CA. **n=1, one model, and a different company from the original** — encouraging, not a measured rate. The same turn surfaced two NEW defects (unguarded bare-blank `replace`, no-op cards) — fixed on the next branch. **Not included: the `docIdentity.ts` unsaved-doc fix** — I had called it a one-liner and that was wrong; see its follow-up row for why it needs its own branch and a live smoke. ⚠️ backend change — `start.sh` restart required. |
| **`chat_history` gated off `contract_review`** | `fix/chat-history-bleed-contract-review` | Closes the re-rated High follow-up: a re-review silently lost a signature-block blocker because Chat-tab turns reached the Findings-tab prompt. New `_HISTORY_FREE_TASK_TYPES = frozenset({"contract_review"})` in [graph/nodes/llm_caller.py](graph/nodes/llm_caller.py) empties `chat_history` before the splice (and logs the suppression count, so traces show it). **Rationale is a design property, not a measured rate** — a document review must be deterministic in its inputs; the same document must yield the same findings regardless of what was said in another tab. **Scoped to the review path only.** Verified that `task_type` is written *only* by `intent_router` and `planner`, both upstream of `skill_dispatcher`, and nothing between the skill node and `llm_caller` mutates it — so `task_type=="contract_review"` at that point is an exact proxy for "the review node ran". The conversational Chainlit skills (`drafting`, `compliance_check`, `contract_generation`) keep the injection — multi-turn continuity is the point there, and they were outside the agreed blast radius. `legal_research` never reaches this node at all (it sets `llm_response` itself; visible as 0-token `llm_caller` GENERATIONs). +2 tests (403 total), **both mutation-proven**: emptying the frozenset fails the suppression test, adding `drafting` to it fails the keep-history test. **VERIFIED LIVE 2026-08-13 (trace `4faadc9f`).** Same `session_id` `8b22698d` as three preceding Blizzard chat turns, review run six minutes later: `llm_caller` received exactly **3 messages** (playbook system, output-constraints system, user) — pre-fix this shape was 9 — and the probes for `Blizzard` / the chat turns / the assistant's replies all came back absent. Substantively the review returned **3 Missing Context rows including MC-003 Execution / Signature Block**, quoting `Signed by: [__]`, `Title: [__]`, `for and on behalf of [__]` — the exact finding the bleed used to delete — matching the clean baseline cell ① rather than the degraded cell ②. `Overall status: Not ready`. **Deliberately shipped before the eval harness, not after** — the pre-fix baseline is `fd93504`, a known, reproducible, blocker-dropping regression that any future eval covering this path ought to be able to detect. **Correction (2026-08-20):** the Tier 1 harness that shipped (`feat/eval-harness`) is zero-LLM and deterministic by design, and this regression is a full-review LLM output change — it was never a candidate for that corpus and isn't in it. Measuring it needs either a live smoke (as done here) or a future Tier 2, LLM-scored eval. ⚠️ backend change — `start.sh` restart required. |
| **Repo cleanup + `legal_research` five-module split** | `chore/cleanup-and-legal-research-split` | Two behavior-neutral tiers. **Tier 1:** deleted dead `skills/schemas.py` (86 lines, zero importers); archived the five shipped `00_PLAN`/`01`–`04` step docs to `docs/archive/` with SHIPPED banners; corrected the stale wiki header (380→401 tests, 161→165 asserts) and architecture diagram (`Tracing → Langfuse` → `Tracing → OTel (Langfuse local / Phoenix VM)`); pruned the roadmap 52→42 rows (8 struck-through DONE rows already duplicated in Shipped, plus 3 overlapping harness rows merged into one); made the 7 hand-rolled `clients/word/src/*.test.ts` scripts **able to fail** (each defined a local `pass()` that only `console.log`'d, so every file exited 0 regardless — now a shared `testAssert.ts` sets `process.exitCode = 1`, using a module-scoped `declare const process` rather than `@types/node`, since tsconfig pins `"types": ["office-js","vite/client"]`); added **`scripts/check.sh`** as the pre-push gate (pytest + `tsc --noEmit` + all 7 assert files + a 165-assertion count check). **Tier 2:** `skills/legal_research.py` (952 lines) → the package `skills/legal_research/` — `prompts.py` 103 · `edit_parsing.py` 193 · `review_recall.py` 272 · `context.py` 181 · entry `legal_research.py` 296. Cut along the pure/impure line: **three of five modules import nothing from this project**. Matches the `contract_review/` + `contract_generation/` house pattern, so `graph/graph.py:25` is byte-identical. **Verification:** all 33 moved symbols AST-proven byte-identical; the 4 prompt constants sha256-identical to their pre-split baseline; `git diff main...HEAD -- graph/ api/ word.ts parser.ts` empty; 401 tests + 165 asserts throughout. **The mutation check earned its keep** — it caught **two tests passing vacuously** (they patched `get_settings` on the entry module after the code path moved to `context.py`; the attribute existed on both, so the patch succeeded and did nothing — a green suite would have shipped them). **New gotcha, now in CLAUDE.md:** `__init__.py`'s re-export shadows the submodule attribute, so `import skills.legal_research.legal_research as X` binds the **function**; tests must use `importlib.import_module`. `patch("…")` strings are safe (they resolve via the import machinery). Pre-exists in `skills/contract_review/` too. ~15 names cross a module boundary and are therefore silently-no-op-able in one direction — the general rule, not one exception. **Smoke (Word sideload, NDA, 2026-08-13):** steps 1/2/4/5 green — span tree unchanged, lean-vs-grounded gate correct both ways (4531 vs 11002 tokens), redlines stripped, 2 edit blocks parsed → 4 applyable cards, review recall exact 1:1 against 5 stored findings, reconciliation correctly conservative (nothing applied → nothing dropped). **Step 3 (SOW) since run green**: contract type `sow` (16 `SOW-0` clause hits, 0 NDA/MSA/BAA), governing MSA attached + truncation branch exercised, grounded gate fired, 15 730 input tokens, and the answer quoted MSA §2 precedence verbatim — so **every branch in `context.py` is now exercised live** (lean + grounded gate, playbook-only + playbook+MSA, with + without a prior review, MSA truncation); only `_cap_chat_context`'s over-budget path remains untested (needs a >100k-char assembly). That step also surfaced the MSA-truncation false-finding issue — see its row below. **The smoke surfaced three model-behavior problems, none caused by this branch** (all on untouched code) — see the re-rated `chat_history` row and the two new rows below. Spec: `docs/superpowers/specs/2026-08-12-cleanup-and-legal-research-split-design.md`; plan: `docs/superpowers/plans/2026-08-12-cleanup-and-legal-research-split.md`. |
| **Per-user identity (name bridge → SSO)** | `feat/per-user-identity` | Self-entered display name: `X-User-Name` header → `state["user_name"]` → `audit_log.user_name` + Langfuse trace metadata. **Stable id stays the memory key — no re-keying.** "Your name" field in the Preferences tab; dev/prod manifests coexist (distinct dev `<Id>`, "Legal Triage (Dev)"). SSO teed up: dormant `resolve_user_name` token-`name` branch + Azure app-registration request (`docs/deploy-it-request.md` Request 3). Short/long memory verified per-user on the VM. Follow-ups: tracing-UI deploy (Langfuse doesn't fit the VM — `audit_log` is the interim), RAG corpus seeding, SSO execution. |
| Within-session conversation memory | `feat/within-session-memory` merged 2026-05-20 | `chat_history` field with idempotent reducer; N=5 turns / trim=300 chars; RedisSaver-backed |
| Redis checkpointer wired to graph | shipped with memory feature | `redis/redis-stack-server` (RediSearch + ReJSON) with auth |
| Resume after interrupt | `feat/resume-after-interrupt` merged 2026-05-21 | 4-way verdict (approve/revise/loop/cap), `Command(resume=...)`, 24h TTL refresh |
| Iterative review loop | shipped with resume | Up to 3 attorney-notes iterations; revise-not-regenerate via `previous_draft`; cap surfaces unincorporated notes |
| Revision speedup | shipped with resume | Loop-back uses direct ChatOllama call with `reasoning=False` instead of full ReAct agent |
| PDF download on approve | shipped with resume | `cl.File` element attached to final report |
| Chainlit review buttons | shipped with resume | Migrated `cl.action_callback` → `cl.AskActionMessage` to dodge Chainlit's same-name disable on loop-back |
| `clients/` regrouping | shipped with Word add-in | `frontend/` → `clients/web/`; new client surfaces land as sibling dirs under `clients/` |
| **Word add-in MVP (Iteration 1)** | `feat/word-addin-triage` | React + TS + Vite, sideloaded into Word for Mac via the `wef` folder. Reads doc body via Office.js → `POST /api/query` (`task_type=contract_review`, `uploaded_text=body`) → renders per-clause cards with severity badges, current text, suggested redline, rationale. Zero backend changes — parses the existing `contract_review` markdown via regex. |
| **Word add-in interactive assistant (Iteration 2)** | shipped with add-in | Each card gets "Show in document" (scrolls + Word Comment) and "Accept redline" (Track Changes replace). Multi-paragraph clauses handled via head + tail snippet search and `range.expandTo()`. New Chat tab uses the same `session_id` so `chat_history` carries the prior `contract_review` forward into Q&A turns. `legal_research` patched to read `uploaded_docs` and respond conversationally (no structured headers) when a doc is attached. Tabs stay mounted with `display:none` toggling so state persists across switches. |
| **Word add-in: chat-driven document edits (Iteration 3)** | `feat/word-addin-chat-edits` | Chat is now a drafting partner. When the lawyer asks for a change, `legal_research` emits fenced ```json``` edit proposals (`replace`/`insert`/`delete`) alongside its prose; the pane renders a preview card per proposal with an editable "after" textarea and **Apply with Track Changes**. `risk_assessor` no longer flags "no chunks" as high-risk when a doc is attached (that spurious interrupt was dropping chat output). Matching hardened: shorter word-prefix + clean-prefix search candidates, match-boundary ranges (fragment rewrites replace exactly the fragment), `insertParagraph`-based inserts. Light styling polish (Office `#0078D4`, Segoe UI, focus rings). |
| **Trinetix legal-team playbook integration (Phase 4)** | `feat/playbook-integration` | `contract_review` is now powered by the legal team's real materials: `Trinetix_Contract_Playbook_2026.docx` (11 tables) + per-type `SKILL.md` (NDA / MSA / SOW / BAA) + shared `references/`. `scripts/build_playbook.py` parses the .docx → `skills/contract_review/playbook/{global,nda,msa,sow,baa}/` markdown (committed for audit; idempotent on re-run). `skills/base.py::load_bundle()` concatenates the bundle in deterministic `BUNDLE_ORDER` (role → principles → risk rating → approval matrix → output format → AI procedure → external comments → catalogue → per-type rules → No-Signature Gate last). The skill detects contract type from heading keywords and stores it on `state["contract_type_detected"]`. Output format switches to the team's required tables (Review Summary + Key Findings + Red and Missing Context + Suggested Redlines + Business Questions + **No Signature Checklist Result**). Word add-in parser rewritten for GFM tables; FindingsTab renders a No-Signature Gate banner (green if "Signature may proceed", red if "DO NOT SEND FOR SIGNATURE") + per-finding owner / required-action / issueId; Blockers card derived as a strict subset of Key Findings (rating ∈ {Red, Missing Context}) + collapsible Business Questions section. See `docs/playbook_cross_reference.md`. |
| **Word add-in chat reliability + `replace_all` action** | shipped with playbook integration | Hardened the chat-driven edit pipeline against local-LLM quirks: tolerant JSON parser (escapes raw `\n` / `\t` inside JSON string values), accepts arrays inside a single fenced block, Ollama `format='json'` mode for the retry path, past-tense edit-promise detector, yellow safety-net warning when prose promises an edit without a block, raw-response viewer in the chat tab for diagnosing LLM output. New `replace_all` edit action: one block, snapshot all matches via `body.search` BEFORE any modification, iterate the snapshot — bypasses the Track Changes "deletion-marked original still visible to body.search" trap that broke earlier loops. `simplifyMultilineReplace` collapses `target/new` pairs that differ on one line down to a clean single-line replace. Match-completeness threshold (85%) refuses partial-prefix replacements that would inject `new_text` into a short prefix. `legal_research` for doc-attached chats switched from ReAct agent to direct ChatOllama call — multi-minute → ~15-30 s per turn on local Ollama. |
| **Main-flow audit + Fix #1: contract-type detection** | `fix/contract-type-detection` merged `ad177c0` | Full audit of graph flow / skill classification / system-prompt duplication / data-caching / output quality / Langfuse — real-trace case studies in the plan file; conflict report in `docs/output_format_conflict.md`. **Fix #1:** `_detect_contract_type` scored keyword hits over only the first 4000 chars, flat-weighted — a real MSA scored SOW (14 vs 12 in-window) and loaded the SOW playbook silently. Now weights the title region (first ~200 chars) ×100 and scans the whole doc as a tiebreak → MSA 469 vs SOW 154. Also tags `contract_type_detected` on the Langfuse trace (detection was previously invisible — the skill has no `@observe` span). |
| **Fix #2: output-format conflict (Option A)** | `fix/output-format-conflict` | The bundle handed the model three competing output specs: `output_format.md` (the endorsed 7-section format) plus the `.docx` §10.2/10.3/10.4 "AI output schema" blocks (issue-list / executive-summary / SOW-readiness). Source audit confirmed parsing is faithful and the conflict is a **source-document ambiguity** (operating rules + every `SKILL.md` + every `test_prompt.md` endorse the 7-section format; the §10 schemas are unendorsed). `build_playbook.py::_render_ai_review_procedure` now stops at the first "AI output schema" subsection (keeps §10.1 behavior rules); the regenerated bundle carries a single output spec. Rollback steps + the open legal-team source fix are recorded in `docs/output_format_conflict.md`. |
| **Fix #3: system-prompt noise cleanup** | `fix/prompt-noise-cleanup` | Two prompt-noise removals, source untouched. (1) **Build-notice artifacts:** every bundle file's `<!-- GENERATED by … -->` header was leaking into the assembled prompt (~24 comment lines/review); `skills/base.py::_clean_for_prompt` now strips leading HTML comments at assembly time — the files on disk keep the notice for humans. (2) **Dangling `references/*.md` pointers:** each per-type `SKILL.md` told the LLM (which has no file access) to "Use `references/shared_operating_rules.md` / `references/no_signature_checklist.md`" — but that content is already inline. `build_playbook.py::_rewrite_reference_pointers` rewrites the 3 pointers to "…in this playbook" while preserving intent. Assembled prompt now has zero `<!--` / `references/`. Rollback inline in both files. |
| **Fix #4: verdict-aware safety gate (Option A)** | `fix/review-verdict-gate` | The human-review gate keyed on the **wrong signal**: `risk_assessor` set `risk_level` purely from citation grounding, so a contract review (doc uploaded → no citation flags) always scored `low` and `route_risk` skipped `human_review` — even when the verdict was **"Do not send for signature."** Now `risk_assessor` branches on `task_type=="contract_review"` and reads the **verdict** via `_assess_review_verdict` (high if "do not send for signature" OR any Red / Missing Context finding; medium on Yellow; low when clean; high if empty) — detection mirrors `parser.ts` so server + client reconcile. Sets `report.requires_attorney` for **both** clients. The `interrupt()` only fires for resume-capable callers: new `interactive_review` request flag (Chainlit sends `True`; Word leaves `False`) gates the contract-review interrupt — Word has no resume UI and shares one `session_id` across review + chat, so an interrupt there would strand the session. Research/generation/drafting routing unchanged. Adds a Langfuse trace tag (`review_risk_level`, `requires_attorney`). +17 tests. |
| **Placeholder / signature-block recall — Layer 1 (prompt cue)** | `fix/placeholder-recall-cue` | The review intermittently dropped unfilled signature-block placeholders (`[__]`). Root-caused with a **faithful A/B**: reused trace `01d4e2ae`'s exact captured prompt + contract, varied a single thing, called ChatOllama at temp 0. Restoring the §10.2–10.4 block that **Fix #2 deleted** deterministically flipped the signature block from a passing **prose** mention to a structured **Missing-Context blocker** — `DELETED→PROSE ×6`, `RESTORED→FULL ×6`, zero crossover, byte-identical per arm. The originally-guessed cause (two-column block masking the empty client column) was **disproved** — the identical block is caught in a shorter NDA and missed in the 129KB MSA (`f4b383f0`), so it's document length + LLM recall variance, not structure. **Real cause:** Fix #2 correctly removed competing output *schemas* but discarded the only completeness *cues* riding in them — §10.2 `Current wording / issue:` and §10.4 `Open placeholders:`. Layer 1 re-injects the **cue, not the schema**, in the tracked `build_playbook.py` (the canonical source under `data/` is gitignored, so the build script is the only reproducible seam — same place Fix #2 made its change): a placeholder / blank-field / unfilled-signature-block bullet in the No-Signature gate's **Automatic blockers** list (highest-attention end of prompt, where it was absent) + a "Completeness and current wording" instruction in `output_format.md` (quote current wording per finding; raise every placeholder as its own Missing-Context row). Validated against the **real shipped bundle** (`load_bundle("nda")` minus the cue is byte-identical to the deleted arm → single variable) → `LAYER1→FULL ×3` deterministic, and **live-smoke-tested across all four contract types (NDA / MSA / SOW / BAA) — now finds blank signature blocks**. +2 build tests (200 total). Toggle for validation/rollback: `PLAYBOOK_PLACEHOLDER_CUE=0 python scripts/build_playbook.py` regenerates the pre-Layer-1 "first variant" prompt; default rebuild restores it. **Still LLM-dependent (a stronger instruction, not a hard guarantee)** — Layer 2 (deterministic scan) remains the floor for the long-doc / recall-variance tail; see follow-up. |
| **Fix #5: faithful document extraction (review/upload path)** | `fix/faithful-docx-extract` | Chainlit's contract-review upload path called the **RAG chunker** (`parse_docx`/`parse_pdf`) then rejoined the chunk text — inheriting the chunker's lossy heuristics. Proven on a real NDA: `parse_docx` mis-classified numbered clause bodies as section headings and **silently dropped 4 substantive clauses** (the Confidential-Information exclusions list, an indemnification sentence, the IN WITNESS execution block) — 89% coverage. That's why the same file reviewed in Chainlit (lossy) vs the Word add-in (Office.js `body.getText()`, faithful) produced contradictory findings. New `ingest/parsers/plain_text.py` provides lossless, order-preserving extractors (`extract_docx_text` = every paragraph + table; `extract_pdf_text` via pdfplumber; `extract_document_text` dispatches by suffix). `clients/web/app.py::_extract_file_text` now uses it → 100% coverage, matching Word. **The RAG ingestion chunker (`ingest/pipeline.py` → `/api/ingest`) is deliberately untouched** — it is correctly tuned for the policy corpus (52-file compliance set: ~96% mean coverage, numbered items are short headings). +7 tests. (RAG-side chunker fix tracked as a follow-up.) |
| **Langfuse GENERATION spans + per-call token usage + skill spans** | `feat/langfuse-generation-spans` | Prereq for the multi-LLM eval: traces had **no token / cost / model** (every node used a bare `@observe`, i.e. SPAN-type), skills had **no span**, and the Word chat-tab LLM call + both ReAct agents were **invisible**. Two mechanisms matching the two call styles, both via the native v2 SDK (no LangChain handler — that needs the full `langchain` package, absent here). (1) The httpx nodes (`llm_caller`/`intent_router`/`planner`) now mark `@observe(as_type="generation")` and attach Ollama's `prompt_eval_count`/`eval_count` as usage via `observability/tracing.py::ollama_usage` (planner previously recorded no observation at all). (2) The five skills get their own `@observe` span (`capture_input/output=False`), and the skill LLM calls run inside `traced_invoke`/`traced_agent_invoke` — an `@observe(as_type="generation")` wrapper that reads token usage from the returned `AIMessage.usage_metadata` (populated by `langchain-ollama` from Ollama's eval counts). The previously-invisible doc-chat call now shows as a nested GENERATION with tokens + model. v2 SDK reports cleanly into the v3 Langfuse server (`langfuse/langfuse:3`). +11 tests (211 total). Cost stays null until a per-model price is registered in the Langfuse UI (follow-up). |
| **Word add-in: tracked-change lifecycle (see-through / communicate / finalize)** | `feat/tracked-change-lifecycle` | Three fixes to the redline lifecycle, found while auditing chat-over-document. (1) **Extraction fix (`13f35da`):** `readBody()` returned `body.text`, which includes tracked-change *deletions* — a placeholder filled via redline (`[__]`→`Suzy Quatro`) still carried the struck `[__]`, so re-review/chat flagged already-filled fields as unfilled (false "DO NOT SEND" blockers; proven by traces `5f188799`→`3b7624b3`, where date/title/signed-by flipped from "missing" to correctly-filled and only genuinely-blank fields remained). Switched to `body.getReviewedText("current")` (text as if all changes accepted; WordApi 1.4, cross-OS; with no changes equals `body.text`, safe drop-in). Covers **both** review (FindingsTab) and chat (ChatTab) — both call `readBody`. `body.search` still matches the raw doc, so redline matching is unaffected. (2) **Lifecycle clarity (`65b1619`):** the applied edit card now states the change is a tracked change that is NOT final and persists through Save until accepted/rejected in Word's Review tab (was a terse "Applied — see Track Changes"). (3) **Finalize → clean copy:** new document-level footer (`FinalizeBar`, shows on both tabs) → `finalizeDocument()` = `body.getTrackedChanges().acceptAll()` + `changeTrackingMode=off`, **in place**, behind an in-pane confirm; the user then does Word's File → Save As. In-place (not separate-file export) because Office.js can't reliably save-as a new named file cross-OS; accepts ALL tracked changes, not only the assistant's (add-in edits are attributed to the current Word user, indistinguishable from manual edits by author). Also recorded (Step 0): Office.js has **no API to recolor tracked changes** — green-insertions / red-deletions is a per-reviewer Word display preference, not add-in code. Frontend-only + one Office.js helper; tsc clean, frontend checks green. Headed to legal-team test for feedback (UX/logic may change). |
| **Word add-in: multi-line signature-block fills (MSA/SOW)** | `fix/multiline-signature-fill` | Chat "fill all blank signature blocks" worked on NDA but errored on MSA/SOW with *"Couldn't find the exact target text."* **Root cause:** MSA/SOW have multiple signature blocks, so the LLM collapses each into ONE `replace` whose target is three `label: [__]` lines, all differing (`Signed by: [__]\nTitle: [__]\nfor and on behalf of [__]`). That target is unapplyable — `body.search` can't cross paragraph breaks, so only the first line matches (~15 chars vs ~52 intended) and the 85% completeness guard correctly refuses; it would also only ever fill the FIRST block. NDA worked only because it has one block (matchable per-field edits). Proven by traces `02e41ead` (MSA) / `ce45b899` (SOW). **Fix (parser, model-neutral):** `normalizeProposals` in [parseEditBlocks.ts](clients/word/src/parseEditBlocks.ts) (a) **splits** a multi-line `replace` whose changed lines are ALL labeled blanks into one `replace_all` per field, and (b) **collapses** the LLM's duplicate per-block cards (main + appendix) into one fill-every `replace_all` — `replace_all` because the blanks recur across blocks and `replaceAll` snapshots all matches in one pass (no struck-text re-find). The result is 3 clean `REPLACE ALL` cards, each filling both blocks' blank column and leaving filled columns untouched. **Key gotcha:** the first attempt lived only inside `extractEditBlocks` and had **zero effect** — [ChatTab.tsx](clients/word/src/components/ChatTab.tsx) prefers the BACKEND's `proposed_edits` over the frontend's extraction when non-empty (the common case), bypassing the transform. `normalizeProposals` now runs on the FINAL chosen list in ChatTab (idempotent). +8 frontend assertions (51 total in `parseEditBlocks.test.ts`); tsc clean; live-smoke-confirmed on MSA & SOW. |
| **SOW reviewed against its governing MSA** | `feat/sow-vs-msa-review` | When `contract_review` detects a **SOW**, it auto-pulls the governing **MSA** from Qdrant and injects it so the SOW is reviewed against its parent — closing a gap where the SOW playbook *required* a "conflicts with MSA" check (`sow/SKILL.md:6/59`, MSA-001) and named the MSA as a required input, but the system never put the MSA in context. **Backend-only, strictly additive** — no Word/API change; non-SOW, no-MSA-on-file, and lookup-error all degrade to the prior standalone review. New `rag/related_docs.py::get_parent_msa(client_id)` scrolls Qdrant (`doc_type="msa"` + `client_id`), sorts chunks by `chunk_index`, returns `(title, full_text)` (picks first by `doc_id` when >1 — one-MSA-per-client demo assumption). `contract_review` (SOW path only) appends a `--- GOVERNING MSA (title) ---` block to the user message (capped at `_MSA_MAX_CHARS=24000`, truncation-marked) + a **structural, model-neutral** `_MSA_COMPARISON_DIRECTIVE` as the last system message — it orchestrates the comparison and defers ALL legal judgment to the playbook (mirrors MSA-001 / SOW:59 precedence rules verbatim; forbids inventing MSA terms; **SKILL.md stays the ceiling**, same category as the existing `_OUTPUT_CONSTRAINTS`). Surfaces `msa_attached`/`msa_doc_title` on the trace. Demo prep: `scripts/ingest_demo_msa.py` ingests the model MSA as `doc_type="msa"` (clears prior MSA chunks first — parsers assign random-UUID doc_ids; fails loudly on a zero-chunk parse). +13 tests (227 total). ⚠️ backend change — `start.sh` restart required; run the ingest script once before smoke-testing. |
| **Word add-in: chat edit scope + signature-rewrite/tab matching** | `fix/chat-signature-fill-scope-and-tabs` | Three follow-on fixes from live signature-block testing. (1) **Scope rule (prompt, model-neutral):** primed by `chat_history` (a prior "fill signatures with John Doe" turn), the LLM volunteered an *unrequested* edit overwriting the already-filled counterparty block (Boris Bukengolts → John Doe) "to ensure consistency" — and admitted going beyond the request (trace `4b24ca1d`). No clean code guard (code can't distinguish a wanted "change Boris to X" from this; "new_text already in doc → drop" would wrongly block the legit multi-block fill). Added a SCOPE rule to `CHAT_SYSTEM_PROMPT` + `_JSON_RETRY_SYSTEM` — do only what's asked, don't mirror a prior value "for consistency", never overwrite a field already holding a real value; "fill" = an EMPTY placeholder. (`chat_history` is *kept* — it's how "Legal name the same we filled recently" resolves.) (2) **Generalized the multi-line split** (`splitMultilineBlankFills` → `splitMultilineFieldEdits`): now splits any multi-line block whose changed lines are structured fields (colon-label OR blank), `replace_all` for blanks / `replace` for specific filled values — so an explicit signatory **rewrite** (Boris→Suzy Quatro, company unchanged) applies as two per-line replaces (trace `32deb028`). Multi-paragraph **prose** (no per-line colon/blank) is left to the head+tail span matcher. (3) **Tab-segment reduction** (`reduceTabSegment`): a `…dotted…\tSigned by: [__]` target that `body.search` can't cross is reduced to the changed column `Signed by: [__]` (trace `9e5b804c`). +13 frontend assertions (64 total) + 1 backend prompt test (214 total); tsc clean; live-smoke-confirmed across fill / effective-date / preamble-edit / signatory-rewrite. ⚠️ the scope rule is a backend change — `start.sh` restart required. |
| **Chat memory & grounding** | `feat/chat-memory-grounding` | The Word Chat tab now persists each completed review to **SQLite** (`memory/review_store.py`) and recalls it in chat — replacing the old 300-char history stub with the real prior review findings. Reviews are keyed to a stable **`document_id`** (`memory/document_id.py`, preamble hash of the uploaded doc — interim; Office.js custom-document-property `document_id` is the durable upgrade). The playbook bundle and the governing MSA are now attached on the **chat path** via the shared `skills/grounding.py` helper (also adopted by `contract_review`, consolidating the two previously-divergent assembly paths); the prompt is assembled **stable-grounding-first / question-last**. **Latency hardening (live-smoke-driven):** heavy grounding is **conditional** (`config.chat_conditional_grounding`) — the playbook+MSA attach only when `_needs_grounding(question)` matches an edit / firm-position / MSA-conflict / clause keyword, so plain factual Q&A stays lean (~10s vs ~30s grounded); and every grounded LLM call now pins `config.ollama_num_ctx` (32768) — without it Ollama's ~4k default silently truncated the ~13.6k-token grounding out of context (middle-drop). (Ollama cross-turn prefix-cache reuse does **not** engage on this local setup even with `OLLAMA_NUM_PARALLEL=1`/`KEEP_ALIVE=-1`, so conditional grounding — not caching — is the real latency lever.) Degraded storage is surfaced loudly: a **startup-absent OR mid-session** Redis failure degrades `/api/query` to a stateless run + `memory_degraded` → an amber Word banner (the turn still answers; grounding/review still load from SQLite/Qdrant); a failed review write surfaces `report["review_persist_error"]` on the Findings tab (never silent). Chat context is capped (`config.chat_context_max_chars`, default 100 000 chars, kept below `ollama_num_ctx`) by truncating the **document**, never the grounding (playbook / MSA / findings). Also strips the prior review's `Suggested Redlines` section from the chat injection so chat doesn't re-propose fills. Verified via live smoke on SOW + NDA (both grounding paths). +44 tests (271 total). ⚠️ backend change — `start.sh` restart required; run `scripts/ingest_demo_msa.py` once if Qdrant was reset. |
| **Word add-in: click-to-jump navigation + findings filter/sort** | `feat/word-addin-quick-ux` | Frontend-only "Bucket A" UX wins (spec + plan in `docs/superpowers/`, sourced from `docs/legal-agent-upgrade-research.md` competitor items #1/#8). New `goToClause()` (`word.ts`) selects a clause **without mutating** the doc; clicking a finding **title** jumps to it, and the old "Show in document" button is renamed **"Comment in doc"** — the audit-trail comment is now opt-in, not fired on every look. Chat proposed-edit cards get a **"Go to"**. New pure `applyFindingFilters()` (`findingFilters.ts`, unit-tested) powers a **filter/sort bar** in `FindingsTab` — severity chips, Blockers-only, owner dropdown, sort by severity/clause, "showing X of Y", reset-on-re-review. Summary chips / gate banner / blockers card / business questions stay **whole-review** (only the finding list filters). No backend/graph/prompt/LLM change → review outputs byte-identical. tsc clean; +9 frontend asserts (133 total pass). **Sideload-smoke-confirmed in Word for Mac (2026-07-09):** filter bar (chips / Blockers-only / owner / sort / "showing X of Y"), click-to-jump, and Comment-in-doc all work. Smoke also surfaced a **pre-existing** clause-locator weakness on placeholder / short anchors — not caused by this change (it reuses the existing locator), tracked as a follow-up. |
| **Word add-in: clause-locator hardening** | `feat/clause-locator-hardening` | `searchFirst` now searches **whole-word-only** for **single-word** anchors via new pure `shouldMatchWholeWord()` (`word.ts`, unit-tested) — `"Title"` no longer matches inside `"entitled"`. (+21 frontend asserts this branch: 7 `word.ts` + 14 `parser.ts`; 154 total project-wide.) **Narrowed to single-word during final review** (approved design was ≤2-word): a single-word query has no space, so Office.js `matchWholeWord` is well-defined; on a space-containing 2-word query it's unverified and can only *hurt* recall (e.g. would stop `"Data Room"` matching `"Data Rooms"`) while buying nothing, so 2+-word anchors keep the tolerant substring match. `searchFirst` is shared by **all** its callers — the locator path (`goToClause`/`showInDocument`/`acceptRedline` via `findClauseRangeFromAnchors`, plus `deleteClause`) **and** `insertNear` — so the narrowing applies to navigation, comment, redline, delete, and insert-anchor lookups alike; behavior is strictly *safer* everywhere (a single-word target/anchor can no longer land mid-word), and redline keeps its 85% completeness guard. Null-range failures now show a calm `NO_MATCH_MESSAGE` ("this finding describes a section rather than quoting it") instead of "Couldn't locate this clause." **Backtick-anchor fix (smoke-driven, 2026-07-10):** signature-block Missing-Context findings wrap their real doc text in backticks (`` `Signed by: [__]` ``, `` `for and on behalf of [__]` ``), which `extractQuoted` (straight/curly only) missed — so 2 of 3 fell through to a clause-name label not in the doc and showed "nothing to locate," while `"Title"` worked only by coincidence (its clause segment equals the doc label). New `extractBacktickQuoted` (`parser.ts`) feeds **only** `buildAnchors` (kept separate so the `` `old` `` → new redline path is unaffected); the backtick literal becomes the primary anchor and `searchFirst`'s wildcard-escape retry locates the `[__]` blank — all three signature fields now locate. **MSA/SOW bundled-block split (smoke-driven, 2026-07-10):** MSA/SOW emit ONE finding whose backtick current wording bundles the whole block on one line joined by `" / "` (`` `Signed by: [__] / Title: [__] / for and on behalf of [__]` ``); that joined string isn't in the doc verbatim (fields on separate paragraphs) so `body.search` can't match it → "nothing to locate" (NDA worked only because it emits one finding per field). New `splitFieldSegments` (`parser.ts`) pushes each `" / "`-separated field segment as its own anchor — guarded to split only on a whitespace-padded slash and only when ≥2 segments look like a field (colon label or blank placeholder), so `"and/or"` and bundled headings aren't split — and the locator lands on the first real field. (Known limitation: MSA's two signature blocks share identical segments, so both findings resolve to the first block — same class as the deferred `[__]` first-occurrence disambiguation.) The wildcard-retry the original follow-up mentioned was **already live** in `searchFirst` — not part of this change. NOTE: the chat `replace_all` locate path (`replaceAll`) does **not** go through `searchFirst` and still matches by substring — separate follow-up if a short `replace_all` needle ever mis-hits. Frontend-only; review outputs byte-identical. Spec: `docs/superpowers/specs/2026-07-09-clause-locator-hardening-design.md`; plan: `docs/superpowers/plans/2026-07-09-clause-locator-hardening.md`. **Sideload-smoke-confirmed in Word for Mac (2026-07-10, NDA + MSA):** `"Title"` no longer lands in `"entitled"`; single-word heading anchors select correctly; 2-word anchors still locate (substring); the calm message shows for truly label-only findings; NDA's three per-field signature findings and MSA's bundled Signature-Block (Main)/(Appendix) findings all locate/comment on the actual `[__]` blank. |
| **Word add-in: calm not-found message styling** | `feat/calm-notfound-styling` | The benign "nothing to locate" `NO_MATCH_MESSAGE` (a finding that describes rather than quotes a section — nothing wrong, nothing to locate) still rendered in the same **red** `.card-status.error` pill as a genuine failure, reading like one. Tagged the benign case in the **data, not by string-matching**: `Result`'s fail variant gains an optional `notFound?: boolean`; the two **navigation** null-range guards (`goToClause`/`showInDocument`) now return a new `notFound(NO_MATCH_MESSAGE)` helper instead of `fail(...)`. (`acceptRedline`'s null-range stays a **red** `fail` with an apply-specific message — a failed *replace* is a genuine error the user must act on, not the benign navigation case; corrected during smoke testing after an initial pass wrongly made it calm.) `FindingCard.tsx` routes a `notFound` result to a new `notfound` `ActionState` kind, rendered via a new muted `.card-status.info` rule (`styles.css`) — same pill geometry, neutral gray background/text, no red. **Both surfaces are consistent:** the Chat tab's proposed-edit `EditProposalCard.tsx` "Go to" (same `goToClause` null-range path) also renders the benign case in the muted pill (fixed during the whole-branch review, which caught that it still showed red). Genuine errors (Word unavailable, empty text, and `acceptRedline`'s completeness-guard "Couldn't find the exact target text…") are untouched `fail(...)` calls and stay red. Frontend-only; no backend/graph/parser/prompt change → review outputs byte-identical; no new asserts (154 total, unchanged — Office.js/React/CSS behavior is smoke-tested, not unit-tested, same posture as the original calm-message copy change). Spec: `docs/superpowers/specs/2026-07-10-calm-notfound-styling-design.md`; plan: `docs/superpowers/plans/2026-07-10-calm-notfound-styling.md`. **Sideload-smoke-confirmed in Word for Mac (2026-07-13):** benign not-found shows the neutral gray pill on "Go to" / title-jump, while a failed "Apply" shows a distinct **red** apply-specific error ("…nothing to replace here."). |
| **Canonical document UUID (review-store identity)** | `feat/canonical-document-uuid` | Foundation slice of the server-side multi-attorney chat-continuity initiative. The Word add-in mints/reads a UUID in `Office.context.document.settings` (`legalTriageDocId`, new `docIdentity.ts`) and sends it as `document_uuid`; `QueryRequest` carries it, `query.py` seeds `initial_state["document_id"]`, and the single `intake.py` resolver now prefers it over the preamble hash (`state.get("document_id") or resolve_document_id(text)`). Because the review write (`memory_writer`) and chat recall (`legal_research`) already read `state["document_id"]`, both inherit the fix: **review recall now survives placeholder fills / redlines** — the fragility caught in the 2026-07-14 sideload smoke of the reverted localStorage branch (preamble hash drifted `05c297…`→`fae237…` once the opening block was filled). Backward-compatible: no `document_uuid` (Chainlit, unsaved docs) → preamble-hash fallback, unchanged. No migration of old rows; review store stays per-document. +3 backend tests. Spec: `docs/superpowers/specs/2026-07-14-canonical-document-uuid-design.md`; plan: `docs/superpowers/plans/2026-07-14-canonical-document-uuid.md`. **Sideload-smoke-confirmed in Word for Mac (2026-07-14):** deterministic check via `data/legal.db` — a fresh review after the fix saved under a dashed UUID (`1db3f02a-…`) where prior rows were the `fae237…` preamble hash; recall survived a preamble edit (filled `[Legal Name]`→`Sony`), and re-reviewing refreshed the snapshot so chat reflected `Sony`. **Note (backend not auto-reloaded):** the first smoke run showed the OLD hash because `uvicorn` didn't pick up the new Python — restart `scripts/start.sh` after backend changes (as CLAUDE.md warns). |
| **Server-side per-attorney conversation store (slice 2)** | `feat/conversation-store` | Chat conversations persist per `(document_id, attorney_id)` in SQLite (`memory/conversation_store.py`), appended per turn by `memory_writer` and read by `_run_doc_chat` (prefers the durable store over Redis `chat_history`, falls back when empty). Per-install attorney id from `localStorage` via the `X-User-ID` header (`clients/word/src/attorneyIdentity.ts`). Additive to Redis (checkpointer still owns interrupt/resume). +17 backend tests (291 total). **Sideload-smoke-confirmed in Word for Mac (2026-07-15):** after Cmd+S → close → reopen the NDA, `conversation_store` held 8 rows all keyed by the same real UUID pair (`document_id 2ae99ecc-…`, `attorney_id cdaa930d-…`) accumulated across the reopen — proving the doc-embedded UUID stays stable through save/close/reopen and the per-install `localStorage` attorney id keys correctly. The deterministic DB check also **caught a test-pollution bug**: a pre-conversation-store test (`test_does_not_persist_for_non_review_turn`) called the real `memory_writer` with `task_type=="research"` without mocking `init_conversation_db`/`append_turn`, doing a live write to `data/legal.db` every suite run — fixed (mock both; deleted junk rows). **Note:** restart `scripts/start.sh` after backend changes (uvicorn no auto-reload). |
| **O365 SSO attorney identity — backend foundation (slice 3)** | `feat/o365-sso-identity` | Final slice of the multi-attorney chat-continuity initiative — the **backend** half, shipped **config-gated and dormant** (`sso_enabled=False` default → today's `X-User-ID` behavior byte-for-byte, incl. the `anonymous` default). New `api/auth.py`: `validate_token` verifies an `Authorization: Bearer` O365 JWT (RS256 only, `aud`+`iss`+`exp` against the tenant's Microsoft JWKS via a cached `PyJWKClient`) and `resolve_user_id` (a FastAPI dependency) sets `state["user_id"]` from the stable `oid` claim (never email/username/sub). The single seam: `submit_query` swaps its `X-User-ID` `Header` param for `Depends(resolve_user_id)` — everything downstream (`conversation_store` `attorney_id`, intake `client_id` map, Langfuse) reads `state["user_id"]` unchanged, so the swap is a strict, reversible superset. **Strict when enabled** (Decision 1=A): missing/invalid token → **401** (no silent header fallback); JWKS unreachable / non-JSON 200 → **503** (never a leaked 500). Security invariants tested against real crypto (in-test RSA keypair; only the JWKS network boundary mocked): bad signature / wrong aud / wrong iss / expired / `alg:none` / HS256 all rejected. **Deferred** (needs a live Azure app): the SSO-on **client** wiring — `getAccessToken()` + manifest `WebApplicationInfo` + dialog fallback — and the Azure app registration + admin consent. **No sideload smoke this slice** (no client change; SSO stays off). +26 backend tests (317 total); `PyJWT[crypto]` pinned. Spec: `docs/superpowers/specs/2026-07-15-o365-sso-attorney-identity-design.md`; plan: `docs/superpowers/plans/2026-07-15-o365-sso-attorney-identity.md`. |
| **Attorney preference memory (USER.md) — stage 1 of the self-improving harness** | `feat/attorney-preference-memory` | Each attorney gets a durable, human-owned `USER.md` (per-attorney, `data/attorneys/<attorney_id>/USER.md`, path-sanitized `^[A-Za-z0-9_-]+$` via `fullmatch`) that the agent **reads as grounding every turn** and **suggests to** (explicit only); the attorney owns/edits it in a new Word **Preferences tab**. New `memory/preferences.py` (load/save; **fresh disk read per turn — no content cache**, so delete/empty/edit takes effect next turn); shared `skills/grounding.py::{load_attorney_preferences_block, preferences_block_for_state}` (wraps in `_PREFERENCES_DIRECTIVE`, truncates at `preferences_max_chars`, `""` on empty/error — **best-effort, never breaks a turn, never sets `memory_degraded`**); `GET/PUT /api/preferences` (keyed by `resolve_user_id`; 413 oversize, 403 disabled, 400 unsafe id). Injected as its **own system message, early / never last** — chat: after `CHAT_SYSTEM_PROMPT`; review: after playbook, before `_OUTPUT_CONSTRAINTS`, `_MSA_COMPARISON_DIRECTIVE` still last — so **SKILL.md stays the ceiling** (directive + placement). Explicit suggestion: model emits a ` ```preference ` block (`_extract_proposed_preferences` ↔ frontend `extractPreferenceBlocks`, mirrors) → chat card → one-click **Add** (`appendPreference` GET→append→PUT). Config-gated `preferences_enabled`/`preferences_dir`/`preferences_max_chars` (`@lru_cache`'d → config changes need a restart; file edits don't). Subagent-driven (8 tasks, cheap-tier impls + per-task sonnet reviews + **opus whole-branch review = READY TO MERGE**, zero Critical/Important; reviews caught a plan test-assertion bug + hardened `_safe_attorney_id` to `fullmatch` + added the MSA-last regression test). +33 backend tests (**380** total) + `parsePreferenceBlocks` tsx (4) + typecheck clean. **Smoke (Word for Mac, 2026-07-22/23):** cabinet save/persist ✅; prefs injected into both review (`f8788b0b`) and chat (`fc051464`) in the correct order ✅; suggestion→Add→persist ✅ (`fc051464` emitted the block; disk shows the added bullet); an "always flag auto-renewal" pref surfaced a **Yellow** auto-renewal finding a baseline review lacked (`401e2da9` vs `f8788b0b`) ✅; a **relaxing** pref ("don't worry about governing law") did **not** weaken the verdict (still Do-not-send, indemnity still Red) ✅; emptying the file → next turn has **no** prefs block (`fa3a1bf0`) ✅. **Caveat (follow-up below):** the relaxing-pref ceiling test was *inconclusive* — governing law wasn't flagged in the baseline, so suppression couldn't be observed; relaxing prefs are the dangerous class for the harness. Spec: `docs/superpowers/specs/2026-07-21-attorney-preference-memory-design.md`; plan: `docs/superpowers/plans/2026-07-21-attorney-preference-memory.md`. |
| **Chat gate-verdict reconciliation** | `feat/gate-verdict-reconciliation` | Follow-up to stale-recall. After the recalled review's placeholder rows are dropped, its `# No Signature Checklist Result` gate could still read "Do not send — signatures unfilled," which the chat model (told "do not contradict this review") parroted for a now-filled doc. Two new pure helpers in `skills/legal_research.py`: `_surviving_blocker_count` reads the structured `# Key Findings` table (the blocker source of truth, mirroring the Word parser's `deriveBlockers`) and counts surviving Red/Missing-Context rows; `_reconcile_gate_verdict` inserts a `> **Reconciled:**` note naming the filled tokens (annotate, always) and — **only when 0 blockers survive** — downgrades the `Overall status:` line to `PENDING RE-REVIEW`. **Never asserts a signature go-ahead** (no "ready for signature" / "signature may proceed"); a single surviving Red/Missing-Context finding, an unparseable Key Findings table, or a missing status line all fall back to annotate-only with `Do not send` intact; the gate's `Blocking items:` text is never deleted. The row-drop pass now protects the gate section (shared `_HEADING_RE`) so its `Blocking items:` line isn't orphaned. Chat-injection-only (stored review + Findings tab untouched); pure, model-neutral, no prompt/graph/config/client change. **Final opus review caught a dangerous-direction bug pre-merge:** a bolded `**Red**` rating was read literally and missed, wrongly neutralizing a live gate — fixed by stripping `*`/backtick emphasis from the rating cell (not `_`, to keep the `missing_context` spelling). Two residual undercount risks + the "never-green applies to generated text" caveat documented in the spec Risks. +17 tests (347 total). **Smoke:** Path A (deterministic, real chat-injection path, isolated temp DB) green across CONTROL / neutralize / annotate (incl. a bolded `**Red**` survivor correctly *blocking* neutralize). **Path C (Word sideload, 2026-07-21) surfaced a real limitation:** this qwen review writes the No-Signature gate in **prose** ("Unfilled placeholders in Preamble … and Execution block"), not token-quotes, so the gate step **no-ops on it** (fails safe) — and the model's current-document grounding already handled the visible behavior (it did not parrot a filled `Signed by:` as unfilled). Merged as safe belt-and-suspenders; broadening the trigger to prose gates is a measured-need follow-up (below). Spec: `docs/superpowers/specs/2026-07-16-gate-verdict-reconciliation-design.md`; plan: `docs/superpowers/plans/2026-07-16-gate-verdict-reconciliation.md`. |
| **Chat stale-recall reconciliation** | `feat/stale-recall-reconciliation` | Chat recalls the *last* stored review (durable, point-in-time), so a placeholder the review flagged that the attorney **filled after** the review was still parroted as "unfilled." New deterministic, model-neutral helper `_reconcile_review_with_doc` (`skills/legal_research.py`) extracts the placeholder tokens quoted in the recalled review, checks each (normalized, whole-quoted-string) against the **current** `uploaded_text`, and drops the finding rows whose tokens are gone — prepending an honest "Auto-reconciled: N placeholder(s) … removed" note. Wired into `_load_prior_review_block` (now takes `uploaded_text`) before injection; try/except → on any error / empty doc it injects the review **unchanged** and does **not** set `memory_degraded`. **Conservative by construction** (never drops a live blocker): "most-specific-wins per line" (a bare `[Legal Name]` can't out-vote its own filled span), a line is dropped only when **every** placeholder on it is filled, and **only single-marker backtick spans are droppable** (a bundled MSA/SOW `Signed by: [__] / Title: [__] / …` span is left intact, so a partial fill can't vanish a live blocker). Scope: placeholders only — substantive findings, section headings, and the No-Signature gate verdict are untouched; no prompt/graph/config/client change. Known heuristic limitations (accepted): a bracketed *defined term* in a substantive finding, or a field the doc *relabels* verbosely, can over-drop (low-prob; documented in the spec Risks). +13 tests (330 total). **Smoke:** Path A (deterministic, real DB read→strip→reconcile→inject) green; Path C (Word sideload, 2026-07-16) green — chat reported only genuinely-blank fields (Address / Title / entity), never the filled Signed-by / Legal Name / Effective Date. Spec: `docs/superpowers/specs/2026-07-15-stale-recall-reconciliation-design.md`; plan: `docs/superpowers/plans/2026-07-15-stale-recall-reconciliation.md`. |
| **SQLite → Postgres store migration** | `feat/postgres-store-migration` | All three relational stores (`audit_log`, `review_store`, `conversation_store`) moved off SQLite (`data/legal.db`) onto a dedicated `app-db` Postgres container (docker-compose, `127.0.0.1:5434`, `POSTGRES_PASSWORD` from `APP_DB_PASSWORD`). New `memory/db.py`: one app-wide `psycopg_pool.ConnectionPool` (autocommit — stores are single-statement) built from `config.database_url`; `init_db()` creates all three tables + their indexes (idempotent `CREATE TABLE/INDEX IF NOT EXISTS`) and is called once from `api/main.py`'s lifespan startup (consolidating what were three separate `init_*_db()` calls). Each store kept its existing call-site signature (Option A) — only the storage swapped: `%s` placeholders, `BIGSERIAL` ids, TEXT timestamps (unchanged ISO-8601 strings), and `conversation_store.load_recent`'s `max_messages<=0 → []` guard (needed because Postgres, unlike SQLite, rejects a negative `LIMIT`). Tests now require Docker: `tests/conftest.py` starts one session-scoped `testcontainers.postgres.PostgresContainer("postgres:17")`, points `DATABASE_URL` at it, and an autouse `_clean_tables` fixture `TRUNCATE`s all three tables before every test — full isolation without three separate SQLite temp files. `sqlite_path` config field removed (Task 5) along with the last `SQLITE_PATH` env references (`.env.example`, `.env`, `tests/test_config.py`); grep for `sqlite_path`/`sqlite3` across `config.py`/`api/`/`graph/`/`memory/`/`skills/`/`tests/` is clean. No data migration — dev/demo data starts fresh in Postgres. **Deps:** `psycopg[binary]`, `psycopg-pool`, `testcontainers[postgres]` added; `aiosqlite`/`sqlite3` usage dropped. 380 tests green (Docker up). Spec: `docs/superpowers/specs/2026-07-23-postgres-store-migration-design.md` (this is **spec 1 of 2** in the "run on company O365/SharePoint" effort — **spec 2, VM deployment + hosting (reverse proxy, cert, remote Ollama, manifest, sideload), remains the follow-up**, tracked below). |
| **VM deployment — Bucket A (buildable & testable now)** | `feat/vm-o365-deployment` | Spec 2's prereq-free half, mirroring `../compliance-bot`'s container pattern. New `Dockerfile` (python:3.12-slim + build-essential/git, slim `requirements-runtime.txt` — `requirements.txt` minus chainlit + the test stack) builds `legal-plugin-backend:latest`; `Caddyfile` reverse-proxies `/api/*` to the backend and serves the built pane (fallback to `taskpane.html`) for everything else; `docker-compose.remote.yml` is a VM overlay adding `backend` + `caddy` services on top of the existing `docker-compose.yml` infra (`app-db`/Redis/Qdrant unchanged) with in-network service-name env overrides; `.env.remote.example` documents the VM env (Spark `OLLAMA_BASE_URL`, `ADDIN_ORIGIN_HOST`, `APP_DB_PASSWORD`, model/dim matching); `clients/word/manifest.template.xml` + `scripts/build_manifest.py` render a prod manifest from `$ADDIN_ORIGIN` (validates in memory, writes only on success); `clients/word/vite.config.ts` fixed so `npm run build` actually produces the pane bundle. New `docs/deploy-vm.md` runbook ties it together, marking each step Bucket A (buildable now) vs Bucket B (gated). **Bucket B — hostname + trusted cert provisioning, the actual deploy to `SRV-AGENT-01`, sideload to testers, and end-to-end tester smoke — is gated on VPN/cert/surface decisions outside this repo** and remains the follow-up (tracked below). Spec: `docs/superpowers/specs/2026-07-23-vm-o365-deployment-design.md`; plan: `docs/superpowers/plans/2026-07-23-vm-deployment-bucket-a.md`. |

| **Context budget + loud truncation — fixing two silent truncation bugs** | `feat/context-budget` | Two independent silent bugs caused the agent to answer legal questions about text it never saw. Bug 1: `_cap_chat_context` truncates only the document (tail cut), removing the back of the contract — exactly the heavy-weighted clauses (limitation of liability, indemnification, term, governing law, signatures) the No-Signature Gate depends on — with only a backend `logger.warning` signaling it. Bug 2: `contract_review` has no input cap; `ollama_num_ctx = 32768` left only 24,576 tokens for input against a measured 25,270-token MSA review, so Ollama silently middle-dropped the overflow **(removing exactly the playbook/MSA)**. **Root cause:** `ollama_num_ctx` must EQUAL the inference server's resident window, not merely fit under it — a mismatch in either direction forces a 4.7s model reload of a 24GB model on every call. Two fixes: (1) `_cap_chat_context` now returns `{doc_chars, kept_chars, kept_pct}` → `state["context_truncated"]` → `report` → **a red pane notice** (distinct from amber "not remembered" warning); both chat and contract-review tabs show the truncation. (2) Upgraded `config.ollama_num_ctx` from 32768 to 131072 (matching Spark's resident window), eliminating the reload tax and fixing the middle-drop. **Discovery:** chars/token is **4.89 on real legal text, not 4** — a 22% overestimate that made every prior budget comment wrong. Memory was never the constraint; **prefill latency is** (1,397 tok/s measured, leading to a derived `chat_context_max_chars` budget). `ollama_num_predict_review = 8192` at 51.4 tok/s decode = 159 seconds, making the review path's bottleneck decode, not prefill. **Gate detectors now active:** `llm_caller` detects overflow past the review headroom (122,880 input tokens / 600,883 chars) and flags it without truncating (contract_review must be deterministic). +23 tests (515 total). Spec: `docs/superpowers/specs/2026-08-21-context-budget-design.md`; plan: `docs/superpowers/plans/2026-08-21-context-budget.md`. |

---

## Follow-ups / Roadmap

| Feature | Priority | Notes |
|---|---|---|
| **Raising the MSA cap requires solving "the MSA must not absorb history" first** | Medium | Attempted 2026-08-27 on `feat/compaction-decision-log`, **reverted the same day** (`2e12745`, `d0a4b21`) — recorded here because the failure is the interesting part and the next attempt will hit it again. The change was: make `msa_max_chars` a ceiling rather than a size (24 000 → 100 000, so the full 73 152-char MSA is sent), bound it per path by subtraction (`review_headroom_chars − document − playbook`), and have `_cap_chat_context` trim the MSA before the document. All of that is defensible in isolation. **It broke compaction.** The attach-time bound knows the document and the playbook but NOT the history, so the MSA claims every remaining character and the assembly pins itself to the budget. Measured live on two consecutive turns of the same document: turn 1 `16 008 + 30 412 + MSA 36 259 + history 0 + 7 208 = 89 887`; turn 2 `16 008 + 30 412 + MSA 27 712 + history 8 541 + 7 214 = 89 887`. Identical totals — the MSA shrank by exactly the history that appeared. Consequences: `pct` is pinned at 99 and can never fall, so `can_compact` is permanently true; anything a compaction frees the MSA immediately re-claims, so auto fires every ~2 turns forever and never relieves anything. That is precisely the churn loop `compaction_auto_min_chars` exists to prevent, reintroduced through a different door — and it silently invalidates the guard the floor relies on ("a successful compaction drops `pct` back under the warn line"). A second defect in the same attempt: the "spend the MSA only if it saves the document" rule means that at large history the MSA is kept WHOLE while the contract is cut and the prompt is *still* over budget — the worst of the three outcomes, and the opposite of the principle it was written under. That rule was contorted to satisfy a pathological existing test (budget 2 000, MSA 8 chars) instead of updating the test. **What the next attempt needs:** the MSA's bound must be computed from what is left AFTER history and the review block, not before them — which means it cannot be decided at attach time in `_build_chat_grounding`, where neither is known. Either move the decision to the assembly point in `_run_doc_chat`, or give the MSA a share of the budget rather than the remainder so `pct` still moves with history. Worth doing: the 24 000 cap really does answer MSA-conflict questions from 33% of the MSA, and `feat/context-budget` really did leave 4x headroom unused. But it is a design task, not a constant change. See also the corpus-overfitting row: 24 000 is itself a corpus-derived number, measured against a window we no longer run. |
| **Every measured constant here is a property of a FOUR-document corpus** | **High** | Raised 2026-08-27, and the more general form of the bug fixed the same day: `compaction_auto_min_chars` was 20,000 because that was a plausible number *on one afternoon's conversation*, and it turned out to be unreachable by construction. The corpus is the same kind of environment. Everything below was measured against the Trinetix MSA, its model SOW, an NDA and a BAA — four documents, one language, one drafting house, all clean digital text. **Classify before trusting any of them.** *Mechanism-derived (safe at scale, because they describe a format or a protocol):* `compaction_auto_min_chars=4000` (segment overhead), `compaction_keep_recent_messages=2`, `compaction_min_quotes`/`max_quotes`, `ollama_num_ctx` (a coupling to the server, not a choice), `review_headroom_chars` (derived), `db_pool_timeout`. *Corpus-derived (will go stale, in rough order of blast radius):* **`min_confidence_score=0.45` plus `retrieval_top_k`/`reranker_top_n`/`reranker_candidates`** — tuned when "the right document" was 1-of-4; a relevance threshold calibrated on four documents means nothing at four hundred, and this is the first thing that breaks when the corpus grows (see the legal-books row below, which grows it deliberately). **`est_chars_per_token=4.89`** — measured on English legal prose from one drafting house; a Ukrainian-language contract, an OCR'd scan, or a table-heavy exhibit tokenizes differently, and this figure propagates into `review_headroom_chars` and every budget comment in `config.py`. **`ollama_num_predict_review=8192`** — "a real review emits ~4.6k tokens of tables" is a statement about *these* contracts; a genuinely long one gets a silently truncated review. **`chunk_min_tokens`/`chunk_max_tokens`** (50/400) and **`_CHARS_PER_QUOTE_LINE=141`** in compaction.py — both fitted to how these documents are written. **`chat_context_max_chars=150000`** is mixed: the derivation is mechanism (a 30s ceiling divided by a measured prefill rate) but the sanity check beside it — "at 150k the full MSA + playbook + a review block all fit" — is pure corpus. **What to do about it:** not to re-tune now, which would only overfit harder, but (1) prefer derived bounds over constants wherever a subtraction is available — `msa_max_chars` was deleted this way, and `review_headroom_chars` is the pattern; (2) make each remaining corpus constant *say* it is corpus-derived in its comment, so the next person knows which numbers to re-measure rather than trust; (3) treat the first non-Trinetix, non-English, or scanned document as a **re-measurement event**, not a normal ingest. |
| **Index the legal reference books for retrieval-augmented checking** | Medium | Raised 2026-08-27. We hold several legal books that are not indexed at all today; RAG covers only client documents in `legal_docs`. Two distinct uses, worth separating because they have different risk profiles. **(a) Attorney-initiated lookup** — "what does the Civil Code say about liquidated damages here?" — is a straightforward retrieval turn and low risk: the attorney asked, and the answer is quoted with a citation. **(b) Agent-initiated augmentation** — the model decides mid-review that a clause needs checking against statute — is the valuable one and the dangerous one, because it puts a *new source of legal authority* into a prompt whose governing rule is that **SKILL.md is the ceiling** and the model must never improvise from pretraining. A book is not the playbook. If a retrieved passage can contradict the firm's position, the precedence has to be explicit and structural (the way `_MSA_COMPARISON_DIRECTIVE` defers all judgment to the playbook), or we have quietly given the model a second authority to reason from. Design notes for when this is picked up: books need their own collection or a `doc_type` that `client_id` filtering cannot accidentally mix with client material (constraint 3 is about tenancy, and a book belongs to no tenant); citations need book + edition + section, not `doc_id`, or the parser's `doc_id:` grep produces nonsense; chunking legal prose at `chunk_max_tokens=400` was tuned on contracts and will cut statutes mid-provision; and this multiplies the corpus by an order of magnitude, which is exactly the condition under which `min_confidence_score=0.45` stops being meaningful (see the row above). Also a real prefill cost — a retrieved statute block competes with the contract for the same budget, so it should ride the same "trim reference material before the document" rule the MSA now follows. |
| **`_cap_chat_context` can cut the document to ZERO and still be over budget** | **High** | Found on the VM 2026-08-27. The cap protects the grounding by truncating only the document: `keep = max(0, len(uploaded_text) - overflow - len(marker))` ([context.py:221](../skills/legal_research/context.py)). When the overflow originates OUTSIDE the document — an attorney pastes a contract into the chat box, so it lands in `system & question` — that `max(0, …)` floors to zero, the entire contract is dropped, **and the prompt is still over budget**, because cutting the document was never going to close a 93k-char question. Observed: `system & question` 92,999 chars = **103% of budget on its own**, document 16,008 → 0, total 25k tokens against an 18k budget. The model then answered a SOW-vs-MSA termination question — confidently, and wrongly in principle — having read none of the SOW. The pane's red notice fired and said exactly the right thing (*"I could read none of this document"*), so this is disclosed rather than silent, which is the shipped honesty working as designed. But pasting a counterparty document into chat with "compare this against the SOW" is completely ordinary attorney behaviour. Two things missing: a **floor** — an answer built on zero contract should refuse rather than proceed, since a caveated wrong answer about liability is worse than no answer — and a message that distinguishes *"your question is too large"* from *"the contract is too large"*, because today both read as the contract's fault and only one is actionable by the attorney. Related to the fabricated-completion row below: same function, same lever, and this is the more reachable path. |
| **One oversized pasted message degrades every later turn until it is condensed** | Medium | The companion to the row above, same VM session. A pasted document is stored in `conversation_store` like any other user turn (correctly — user rows are byte-identical by design, they are the attorney's own words). It then replays as history on every subsequent turn: measured, history went to **87,282 chars = 96% of budget** on the turn after the paste, and the attached document was truncated to zero *again*. So a single oversized message is not a one-turn problem, it is a persistent tax on the conversation. Compaction is the correct remedy and now fires on it (the size floor, `compaction_auto_min_chars`), but that is a cure rather than a guard: nothing warns the attorney at paste time that what they are about to send will cost them the document on every turn from here. Worth considering a size warning in the composer, or offering to attach a large paste as a document rather than a message. Note the interaction with `conversation_max_messages`: the injected window is a message COUNT, so one enormous row occupies a whole slot and cannot be partially admitted. |
| **Truncation induces FABRICATED "completion" edits at the cut boundary** | **High** | Observed live 2026-08-25 (Word for Mac, `CHAT_CONTEXT_MAX_CHARS` deliberately set back to 100000 to force the notice). `_cap_chat_context` cuts the document at an arbitrary character offset, and that offset landed **inside** the limitation-of-liability clause: clause starts at char 53,743, cut at 54,108, so the model received `"…TWELVE (12) MONTHS OF THE TERM OF THE MSA, TWELVE (12) TIMES THE AVERAGE MONTH"` and then nothing. It then emitted a `rewrite` proposal whose own rationale reads *"Completes the cut-off sentence by inserting the standard Trinetix fallback formula for the first 12 months of the term."* **The model is repairing damage that exists only in our prompt — the real contract's sentence is intact.** One click on *Apply with Track Changes* injects invented text into the highest-stakes clause in the agreement, and the edit looks *more* credible than usual because completing a visibly broken sentence is exactly what a helpful assistant should do. Two aggravating details: the edit card renders **above** the red truncation banner (`ChatTab` puts the banner after the message list), so the actionable thing is read before the caveat; and the fabricated text is plausible firm boilerplate, not obvious nonsense. **Not reachable at the shipped 150,000 budget** — that needs a document around 110k+ chars against a largest-real-contract of 84,859 — so this is latent, not live. But it is latent *by construction*: the whole premise of the context-budget work is that truncation can still happen, and this is what it does when it does. **This is also the cleanest reproduction yet of the open "chat edit path fabricates facts" row** — previous instances were single traces (`0f63a143`), this one is deterministic: set the budget, ask a liability question, get a fabricated completion. Fix direction: a truncated document is not a safe basis for **any** one-click contract edit — suppress edit proposals entirely when `context_truncated` is set (or at minimum refuse ones whose target abuts the cut boundary), and move the banner above the message list. Belongs with the code-side provenance guard rather than bolted on separately. |
| **OpenAI-compatible backend seam** | Slice 2 | `ChatOpenAI(base_url=…)` alongside `ChatOllama`, Ollama default, vLLM behind config. Makes Qwen3-Coder-Next a one-env-var experiment and prerequisite for open "LLM evaluation across models" work. Nothing changes for testers until flipped. |
| **Read the condensed summary in the pane** | Medium | The half of "visible but automatic" that did not ship with automatic firing. Segments are never returned to the client today, so this needs a GET route for a document's `conversation_summary` rows plus a viewer in the meter — an attorney can currently see THAT earlier turns were condensed and how many characters it freed, but not WHICH quotes now stand in for them. Automatic firing makes this more valuable, not less: the condensing now happens without anyone choosing it. |
| **A table-shaped answer is unquotable, so its content vanishes from the summary while its space is still reclaimed** | Medium | Measured 2026-08-26 during the auto-compaction sideload. The compaction gate requires a quote to be a COMPLETE SENTENCE of the row it cites, and a pipe-delimited markdown table row has no sentence terminator followed by a capital — so an 8,355-char table answer (324 pipes) offered **8** quotable boundaries in the entire row and contributed **zero** quotes to its segment. It was 76% of the condensed history. The gate is behaving correctly: it refused fragments it could not vouch for, exactly as designed, and a ~50% drop rate on a table-heavy conversation is expected rather than a regression (the ~9% per-quote baseline was measured on prose). The problem is the consequence: a segment replaces its whole row range regardless of how much got quoted, so the single largest answer in a conversation can end up with **no representation at all** in what the model sees next, while its characters are reclaimed. Nothing is fabricated and the raw rows are never deleted, so this is honest — but it is invisible to the attorney today, which is the strongest argument for the summary viewer above. Options, none yet chosen: teach the gate to treat a table ROW as a quotable unit (its own boundary rule, since a row is a self-contained record); have the segment report which source rows contributed nothing; or leave it and rely on the viewer to make it visible. Do NOT "fix" it by relaxing the sentence rule — that is the guard against meaning-inverting truncation and six review rounds went into it. |
| **Re-summarising older segments** | Low | `compaction_max_injected_segments=3` bounds the prompt but means discussion past roughly sixty turns is no longer represented at all — it stays in the store and is auditable, but the model does not see it. Only worth building if a pilot shows attorneys reaching back that far. Explicit action, never silent. |
| **Self-calibrating chars/token for the live document line** | Low | `charsPerToken = charsSent / tokens.input`, seeded 4.89, EWMA-smoothed, persisted. Auto-adapts if the backend model changes. Backend-fed lines are already exact; only the live estimate drifts. |
| **Close the abbreviation residual in the compaction gate** | Low | An unlisted abbreviation before a capitalised name (`"…from Acme Ltd. Partners only if…"`) still lets a quote stop mid-sentence. Measured: adding company suffixes to `_NON_TERMINAL_ABBREVIATIONS` closes it but costs three legitimate quote shapes (`"…the MSA."`, `"…Acme Inc."`, `"…Main St."` followed by another sentence), which are ordinary here. Now carried as `evals/cases/gate-residual-company-suffix-before-a-capitalised-name.json`, baselined in `evals/baseline.json`, with its price pinned alongside it by `gate-accept-sentence-final-company-suffixes`. Closing it prints `[now-passing]` and fails the gate until someone acknowledges the trade; the two cases report both columns in one screen. Revisit only if a pilot shows it biting. |
| **Cross-session recall node — query stored conversations by argument** | Slice 5 | Gated on `document_id` stability (the unsaved-document identity bug, still open) — if the key churns, recall fragments silently. Must be zero-LLM gated like `_needs_grounding`, since the ReAct path costs minutes. Scope strictly to `attorney_id`; conversations are per-attorney while reviews are shared. |
| **Review-path truncation notice reuses the chat notice's shape — `doc_chars` misnomer** | Low | `context_truncated` on the review path is `llm_caller`'s headroom-overflow dict (`{doc_chars, kept_chars, kept_pct}`), reusing the exact shape `_cap_chat_context` returns on the chat path. On chat, `doc_chars` means "the attached document"; on review, `doc_chars` is the WHOLE assembled prompt (playbook + MSA + document), so surfacing the notice literally on the review tab would misattribute what got cut. Deferred on purpose: reaching it needs a >600,883-char prompt (the current headroom at `ollama_num_ctx`/`ollama_num_predict_review`) against a largest-real-contract measured at 84,859 chars — not reachable by any real document today — and a proper fix (a `reason` discriminator) would touch the shared report formatter and both call sites. Revisit if a real review prompt ever approaches the headroom. |
| Qwen3-14B serving fault | Incidental | 7.5 tok/s decode (a serving fault, not a model property). Not ours but recorded so it is not rediscovered. |
| Review-path bottleneck: decode, not prefill | Incidental | `ollama_num_predict_review = 8192` is **159 seconds of pure generation** at 51.4 tok/s — making the review path's bottleneck decode, not prefill. A different problem from chat, worth its own investigation. |
| **Backend image bakes `.pyc` that a corrupt build-cache layer can poison** | Medium | Hit on SRV-AGENT-01 2026-08-19: every `python` process in the backend container except the running server died with `ValueError: bad marshal data (invalid reference)` reading `pydantic/plugin/__pycache__/_schema_validator.cpython-312.pyc`. Not truncation (7027 bytes, correct magic) and not disk (19G free) — the file was baked into the image by `pip`'s byte-compilation and then **frozen in the build cache**, so every `up -d --build` reported `Built 3.4s`, reused the poisoned layer, and could never fix it. `docker builder prune -f` + `build --no-cache backend` fixed it, verified against a fresh container. **Two traps worth keeping:** the *running* backend was unaffected (it held its modules in memory) and served an entire Word smoke while every new process failed — so the box looked healthy right up until a recreation or reboot, which would not have come back; and `rm -rf __pycache__` via `exec` *appears* to fix it while only writing an overlayfs whiteout into one container's writable layer, discarded on recreation. Always confirm with `run --rm`, never `exec`. **Root cause of the corruption itself is unexplained** — how uvicorn read that exact file successfully while every other process could not is not accounted for by any hypothesis tested. Deferred rather than closed: if it recurs, stop treating it as bad luck and remove bytecode from the image entirely (`pip install --no-compile` + `ENV PYTHONDONTWRITEBYTECODE=1`), costing a couple of seconds of startup compile and immunising the whole class. Runbook entry: `docs/deploy-vm.md` troubleshooting. |
| **`docker compose logs` has no timestamps, and app logs have no request correlation** | Low | Now that app-level INFO actually reaches the log (see Shipped row), the next limit is reading it: lines carry no `turn_id`, so during a busy pilot two concurrent turns interleave with nothing to separate them, and the in-flight `-> ollama` / `<- ollama` pair cannot be matched across them. `turn_id` already exists per request and is already the join key for feedback and traces — threading it into a logging filter would make `grep <turn_id>` reconstruct one turn from the log alone, without Phoenix. Wait for real pilot volume to show whether Phoenix already covers this well enough. |
| **VM deployment + hosting — Bucket B (spec 2 of the O365/SharePoint effort)** | High | Bucket A (Dockerfile, Caddy, `docker-compose.remote.yml`, prod manifest renderer, `docs/deploy-vm.md`) **shipped** — see Shipped row. **Remaining is Bucket B: provision the hostname + trusted cert, deploy to `SRV-AGENT-01`, sideload to testers per surface, and run an end-to-end tester smoke** — gated on VPN reachability, cert/hostname decisions, and which Word surface(s) testers use (all outside this repo). Spec: `docs/superpowers/specs/2026-07-23-vm-o365-deployment-design.md`. |
| **Attorney preference memory → self-improving agent harness (NORTH STAR)** | High (Stage 1 ✅ SHIPPED 2026-07-23) | The project's main forward track. **Stage 1 ✅ DONE (shipped in `feat/attorney-preference-memory` — see Shipped row): per-attorney `USER.md` preference memory** — human-owned markdown per attorney (keyed by `attorney_id`), read as grounding on every review/chat turn, edited in a new Word **Preferences tab**, with the agent **suggesting** additions explicitly (agent-suggests → attorney-confirms; the human is the only writer, so a flat MD file is safe). Subordinate to the playbook — SKILL.md stays the ceiling (directive + early injection placement). Spec: `docs/superpowers/specs/2026-07-21-attorney-preference-memory-design.md`; plan: `docs/superpowers/plans/2026-07-21-attorney-preference-memory.md`. **Stage 2+ (the harness proper, deferred):** **inferred** capture (agent detects valuable patterns across sessions and writes memory *autonomously* — the self-improving loop), broadening from per-attorney prefs to **firm/company-environment** knowledge, and a **structured + semantic substrate** (SQLite rows with provenance/confirmed metadata + the empty Qdrant `memory` collection for relevance-narrowed recall) since autonomous multi-writer memory outgrows a flat file. Stage 1 routes all reads through one `grounding.load_attorney_preferences_block` seam so the substrate swaps in behind it with no redesign. The 'System-wide self-improving harness' and 'Layer 2 safety-net' sub-bullets below are early instances of this program.<br>- **System-wide self-improving harness** (Medium): Every brittle heuristic (contract-type detection, parser `deriveBlockers` / `pick` column aliases / anchor matching, edit-promise `format=json` retry, tolerant JSON) emits a confidence/disagreement signal into one sink for tuning. Free seed: flag when `contract_type_detected` ≠ the model's own stated "Contract type:" line.<br>- **Deterministic placeholder / signature-block safety-net — Layer 2** (Medium, deferred): **Layer 1 shipped + live-validated across all four contract types — this is the remaining *guarantee*.** Cause settled by A/B: the recall gap was Fix #2 collateral signal loss, **not** the two-column masking originally guessed; restoring the cue deterministically recovers structured detection on the NDA (`PROSE→FULL`, 6/6) and finds blank signature blocks on NDA/MSA/SOW/BAA in live smoke. But Layer 1 stays LLM-dependent — it repairs the regression and lifts recall, yet **cannot guarantee** it: the 129KB MSA (`f4b383f0`) missed all 6 placeholders (length-driven), and prose-only runs (`01d4e2ae`) never reach Word's `deriveBlockers` blockers card. Layer 2 = a deterministic post-LLM scan of the faithful `uploaded_text` for unfilled markers (`[__]`, `[Legal Name]`, `[Month] [Date], [Year]`, `[Address]`; **exclude** generated-draft `[Source: doc_id]` tags) that **forces** each into a structured Missing-Context row + "Do not send for signature" (Fix #4 then escalates `risk_level`/`requires_attorney`). Must emit **structured rows**, not just gate prose. Deferred per decision — build when Layer 1's measured recall (esp. on long docs) proves insufficient. A first instance of the self-improving harness. **Denominators now exist (2026-08-14):** `findings_rendered` (`scripts/feedback_report.py` / `interaction_event`) records real per-review finding volume, and a ⚑ on any card means a missed placeholder can be reported the moment an attorney spots one, with the turn attached — a measured recall number is a query away instead of a fresh smoke. |
| **Chat: relaxing preferences vs the ceiling (harness safety)** | Medium | Fell out of the Stage-1 smoke (2026-07-22). Attorney prefs are injected subordinate to the playbook (directive + early placement), and a relaxing pref ("don't worry about governing law") did **not** weaken the review verdict. But the suppression test was **inconclusive** — governing law wasn't flagged in the baseline, so we couldn't prove a relaxing pref can't drop a real playbook blocker. Relaxing-style prefs ("don't worry about X", "ignore Y", "skip Z") are the **dangerous class**, and the risk grows once the agent writes prefs itself (Stage 2). Needs a dedicated eval: a doc where the playbook flags a clause (e.g. governing law) + a relaxing pref, asserting the blocker survives — and consider a guard that tags relaxing-language prefs as non-authoritative. Ceiling is enforced structurally, not provably (LLM-dependent) — same class as the other "SKILL.md is the ceiling" items. **Denominators now exist (2026-08-14):** `preference_added` counts and the in-pane ⚑ (`scripts/feedback_report.py`) mean a relaxing-pref-caused miss now arrives as a `feedback` row with its exact turn attached, rather than depending on someone noticing and writing it up unprompted. The dedicated suppression eval this row asks for still needs to be built — it no longer has to happen blind. **The Tier 1 deterministic harness (`feat/eval-harness`, 2026-08-20) does not cover this** — it runs zero LLM calls by design, and a relaxing-preference suppression check is inherently model behavior (Tier 2, LLM-scored). This row's suppression eval is the flagship Tier 2 case: it is the one place the north star's "guardrail" leg is still unproven rather than merely unbuilt. |
| LLM evaluation across models + clause-content tuning | High | Smoke on real Trinetix NDA / MSA / SOW / BAA passed — the team's table format, No-Signature Gate, and per-type bundles all fire correctly. Next: evaluate on multiple LLMs (qwen vs llama vs others), and tune cases where the model misidentifies clause-by-number (e.g. asked to remove "3.4" but removed "Permitted Disclosures" by name match). Work on a separate `tuning/*` branch. **Idea — Langfuse Prompt Management:** register each per-type assembled bundle as a versioned Langfuse prompt and link it on the `llm_caller` GENERATION (`update_current_observation(prompt=...)`, supported in our v2 SDK via `create_prompt`/`get_prompt`). Gives prompt-version ↔ trace ↔ tokens/cost/score comparison in the UI — turns the ad-hoc `/tmp` A/B harnesses (placeholder-cue, `PLAYBOOK_PLACEHOLDER_CUE` toggle) into first-class persisted experiments, and seeds the improvisation-rate / self-improving-harness scoring. **Use as a one-way mirror only:** push from `build_playbook.py` (hash-labeled, idempotent, flag-gated); the legal-team `.docx` + `build_playbook.py` stay canonical — do NOT enable UI editing as a production source (would fork truth vs "SKILL.md is the ceiling"). Wire up when this eval workstream starts (no value until there are prompt variants to compare). |
| Playbook-adherence ("ceiling") violations — improvised findings | Medium | **Observed, not a code bug.** On the post-#5 NDA review (Langfuse `cad3631b-acc9-4c7c-b76e-70907aa18629`) the model raised a Yellow on the indemnity clause — *"broad indemnity… add standard carve-outs (gross negligence / willful misconduct)."* The NDA playbook has **no** carve-out rule; §11 + NDA-006 take the **opposite** stance — a broad receiving-party indemnity is the *preferred* position (it protects Trinetix, the Disclosing Party here), and the red flag is *weak/nominal* liability. The model improvised a generic commercial-contracting concept from pretraining, against the playbook — a "SKILL.md is the ceiling" violation. (An earlier Word run of the same file correctly did **not** flag it — so it's run-to-run, not deterministic.) Levers: (a) sharpen ceiling enforcement so the model doesn't volunteer un-playbooked clause opinions; (b) score "improvisation rate" (findings with no playbook rule ID) as an eval metric. Pairs with the LLM-evaluation item above. **Denominators now exist (2026-08-14):** `findings_rendered` volume plus a per-finding ⚑ (`scripts/feedback_report.py` / `memory/feedback_store.py`) mean an attorney can flag an improvised finding the instant they see one, turn_id attached — scoring "improvisation rate" now has a numerator source, not just a plan for one. |
| Tracked-change lifecycle — enhancements after legal-team test | Medium | The shipped Finalize is **in-place** (accept all + tracking off, then user Save As). If portability becomes a hard requirement (sending to outside counsel who must see green/red regardless of their Word settings), add a **separate-file clean-copy export** that preserves the redline master (`getFileAsync` → download; reliable on Windows/WebView2, fragile in Word for Mac's WKWebView — needs platform handling). Other deferred UX: **in-pane per-edit Accept/Reject** + "Accept all from assistant" (`getTrackedChanges()` exposes `.accept()/.reject()`), a **redline⇄clean preview toggle**, and a **remaining-placeholders** indicator (the yellow `[Legal Name]`/`[Address]` blanks). Revisit wording/placement after legal-team feedback. |
| **`chat_history` bleed — residual scope** | Low (was High; core fix SHIPPED 2026-08-13) | The review path is fixed (see Shipped row) — this is what's left. (a) [clients/word/src/App.tsx:12](clients/word/src/App.tsx) still mints ONE `session_id` for both tabs, so review turns and chat turns keep sharing `chat_history`; harmless for `contract_review` now that it's gated, but `_load_prior_conversation`'s Redis **fallback** can still surface a review turn into doc-chat when the conversation store is empty. (b) The Chainlit skills (`drafting`, `compliance_check`, `contract_generation`) keep the splice **unverified** — nobody has checked whether multi-turn continuity there is a benefit or the same bug wearing a different hat. (c) **The rate is still unknown** — the original evidence was n=1 per cell, and the fix was justified on determinism grounds rather than magnitude. **Correction (2026-08-20):** the shipped Tier 1 eval harness (`feat/eval-harness`) is zero-LLM by design and does not, and structurally cannot, cover this — measuring how often chat_history bleed changes a real review's LLM output needs a live smoke or a future Tier 2 eval, not this harness. Baseline `fd93504` remains a known regression to check against whichever of those two eventually covers this path. **Denominators now exist (2026-08-14):** `interaction_event` counts chat vs. findings activity per `surface`/`session_id` (`scripts/feedback_report.py`), so how often a chat turn follows a review turn in the same pane session is on record — the actual bleed rate through the Redis fallback still needs the harness, but the exposure it could occur against is no longer a guess.
| **Chat edit path fabricates facts — code-side provenance guard** | **High (prompt mitigation SHIPPED 2026-08-13; the guard is still open)** | Asked only to *"fill the signature block with Suzy Quatro"*, the model also rewrote the Preamble and invented `Sony Corporation, mailing address at 1-7-1 Konan, Minato-ku, Tokyo 108-0075, Japan` — Sony's real HQ, supplied by nobody, arriving as a one-click **Apply** into a legal instrument. Worse than the indemnity-carve-out row below: that improvised an *opinion* into a review, this fabricates a *fact* into a contract. Trace `0f63a143`. **Shipped:** a SOURCES paragraph in both chat prompts (see Shipped row). **Why that is not enough:** the prompt already carried a general "Do not invent facts" rule when this happened — that is a natural experiment showing a prompt line is a frequency reduction, not a bound, and this is precisely the case that needs a bound. **The open work is the guard,** per [[fix-in-code-not-prompt]]: check whether `new_text` introduces *entity-shaped* tokens — capitalised multi-word runs, digits, addresses, dates — absent from (document ∪ chat history ∪ playbook ∪ MSA). Scope it to **facts, not prose**: legitimate fallback language comes from the playbook (a source) and "Suzy Quatro" comes from the user's own turn (a source), while every token of the Sony address fails. Surface it as a **badge on the card** ("introduces text not found in the document"), not a refusal, until the eval measures the false-positive rate — a hard block tuned on n=1 will reject legitimate edits. **Two aggravating primers were present** (the `USER.md` Sony preference and 2 chat turns naming Sony), so this cannot be pinned on the preference alone, but it is the second time that pref sits beside surprising behavior and it needs its own eval case. **Denominators now exist (2026-08-14):** `edit_applied` volume plus the in-pane ⚑ (`scripts/feedback_report.py`) mean a fabricated value now arrives as a `feedback` row carrying `turn_id`/`trace_id` — the code-side provenance guard is still open, but a false-positive/false-negative rate is measurable instead of anecdotal. |
| **MSA truncation — raise the cap / smarter slice** | Medium (was High; directive guard SHIPPED 2026-08-13) | The *false finding* is addressed at the prompt layer — rule 4 of `_MSA_COMPARISON_DIRECTIVE` now tells the model a truncation boundary is our artifact, never an MSA defect (see Shipped row). **Truncation itself is retained by decision** — it is a deliberate load-management choice while we avoid hammering the local LLM, to be removed later. What remains is the quality ceiling: answering an MSA-conflict question from **33%** of the MSA (24 000 of 73 152 chars) is thin regardless of how the model narrates the cut, and rule 4 is LLM-dependent like every other prompt guard. **Headroom is not the constraint — latency is, and it is cheap here.** (Corrected 2026-08-27; the figures below replaced arithmetic written against the old `ollama_num_ctx` of 32 768, which understated the room by 4x and would have sized this fix from the wrong number.) At the shipped `ollama_num_ctx` 131 072 and `chat_context_max_chars` 150 000, the FULL 73 152-char MSA already fits: document 16 008 + playbook 30 412 + MSA 73 152 + system 7 214 = 126 786 chars = 25 928 tokens, which is 20% of the window and leaves 23 214 chars of the budget free for history. The only real cost of removing the cap is prefill: ~9 900 extra tokens at 1 397 tok/s ≈ **7 seconds** per grounded turn on Spark. So (a) is close to free at the shipped budget, and the load-management rationale that justified the cap was measured against a window we no longer run. Remaining options, cheapest first: (a) raise `msa_max_chars`; (b) cut on a **clause boundary** instead of mid-sentence; (d) retrieve the MSA clauses relevant to the SOW instead of a blind head-slice. Shared by both surfaces via `skills/grounding.py::attach_parent_msa`. Trace `85533de4`. |
| Word add-in: unsaved document silently resets all memory | **High (re-rated 2026-08-17; unblocked — sanitisation shipped)** | **Stakes rose with feedback capture.** The 2026-08-17 live smoke ran entirely on an **unsaved** NDA: `document_id` `d418d522…` held across a pane reload (so `settings` works in-memory), but it exists only while that document stays open. Close and reopen and a fresh UUID is minted, orphaning the 3 feedback rows and 17 events recorded against it — permanently unjoinable from any later feedback on the same contract. This used to orphan a stored review and a chat history; it now also orphans the pilot artifacts the feedback feature exists to accumulate, and testers will absolutely work on unsaved drafts. Sequencing note (now satisfied): this mints a fresh `document_id` on every pane reload, which was silently giving local tests clean history and masking the history-priming bug. Fixing identity alone makes every attorney get the durable history the feature promises — and therefore more priming, not less. Land sanitisation first (done), then this. [clients/word/src/docIdentity.ts](clients/word/src/docIdentity.ts) mints a UUID into `Office.context.document.settings`, which persists **with the file**. On an unsaved doc `saveAsync` has nowhere to write, so after a close/reopen `settings.get` returns empty and the client **mints a replacement** instead of returning `""`. The backend's preamble-hash fallback (`intake.py`) — which exists precisely for this case and *would* have matched — never engages, because the client always has something to send. Observed live: reopening an unsaved NDA changed `document_id` `f873fbc3…` → `03fc1567…`, orphaning both the stored review and the per-attorney conversation. **No `memory_degraded` banner fires** — nothing failed, the key just changed. The file header's "Validated to survive a task-pane reopen" is accurate but narrower than it reads (pane reopen ≠ document reopen). **Why it is not a one-liner (corrected 2026-08-13):** the obvious fix — return `""` when `Office.context.document.url` is empty — rests on a property the typings document as *"null if the URL is unavailable"*, which is not exclusively the unsaved case. If `url` is also unavailable for a **saved** doc in some surface (Word on the web, certain SharePoint paths), the fix would silently demote that surface to the preamble hash, which drifts when the review fills the opening block — trading a reset-on-reopen for a drift-on-edit, i.e. re-opening the exact bug `feat/canonical-document-uuid` closed. Two candidates, and the choice needs a real Word smoke across surfaces: (a) gate only the **mint** path on `url`, leaving existing ids untouched; (b) keep today's behavior but **surface** the unsaved state in the pane ("this document is unsaved — review history will not persist across reopen; save it first") — purely additive, zero regression risk, and it addresses the actual complaint, which is the *silence* rather than the reset. **⇒ (b) SHIPPED 2026-08-18 (`fix/unsaved-document-notice`); the reset itself is STILL OPEN and this row stays open with it.** **Live smoke 2026-08-18 (Word for Mac):** blank never-saved document shows the notice on all three tabs; saving it and refocusing the pane clears it. So `getFilePropertiesAsync` does distinguish saved from unsaved on this surface — the wallpaper risk (a notice that fires on every document, training testers to ignore it) did not materialise. Word-for-web / SharePoint-with-AutoSave remain unsmoked; the silent-on-unusable-probe rule means the worst case there is a missing notice, never a false one. What shipped: `isDocumentUnsaved()`/`probeFileUrl()`/`shouldWarnUnsaved()` in [docIdentity.ts](../clients/word/src/docIdentity.ts) + a blue (deliberately NOT amber) notice above the tabs in `App.tsx`, re-checked on window focus so saving the file clears it. **Minting is untouched.** Three judgement calls worth keeping: (i) the probe prefers `getFilePropertiesAsync` and keys on "did we get a usable url **string**" rather than on `AsyncResultStatus`, so an unsaved document (successful probe, empty url — the actual signal) is never confused with a failed probe (no `value` at all), with no dependency on Office's enum object existing; (ii) an unusable probe stays **silent** — warning on a guess would light the notice on every document in any surface where `url` is merely unavailable, and a notice that is always on is wallpaper, so being wrong here means failing to warn (today's behaviour) rather than crying wolf; (iii) it is styled unlike the amber `memory_degraded` banner on purpose — that one means "this turn wasn't remembered", this means "nothing about this document will be remembered after you close it", and blurring them would cost the rarer signal. **Analysis correction while implementing:** candidate (a) would not have fixed the common case anyway — a contract opened from disk *has* a `document.url`, so it mints, and still loses the id on close-without-save; (a) only ever helped never-saved documents. **The durable fix (deferred, needs its own design):** have the backend record a `preamble-hash → uuid` alias and resolve a fresh UUID back to the original when the preamble matches. Rejected for now because of a collision hazard that matters in a legal tool — two SOWs under one MSA can share a preamble, and a false join would recall the wrong contract's review mid-chat. Losing data is safe; cross-contaminating documents is not. Any attempt needs an unambiguous-alias-only rule (refuse to join when one hash maps to several uuids) and its own cross-surface smoke. Note both keys can orphan a document, just at different moments: the UUID dies at close-without-save, and the preamble hash drifts the moment a review fills `[Legal Name]` in the party block — so falling back to the hash trades one silent reset for another. |
| Reconcile playbook `.docx` §10 (legal team) | High | Proper source fix for the output-format conflict — Fix #2 excludes §10.2/10.3/10.4 at build time as a stopgap. Legal should either mark those schemas internal-reference-only or have §10 defer to the operating-rules output format; then the build-script exclusion can be removed. See `docs/output_format_conflict.md`. |
| Source `SKILL.md` assumes file access (legal team) | Low | The per-type source `SKILL.md` reference `references/*.md` as if readable at runtime; our system inlines everything and the LLM has no file access. Fix #3 rewrites those pointers at build time. If legal updates the source, prefer inline phrasing ("in this playbook") over file paths. |
| Contract-type override in Word add-in | Low | Detection is now title-weighted + whole-doc (Fix #1), so mis-detection is rare; an override dropdown ("Reviewing as: MSA — change") in the FindingsTab chip is now defense-in-depth rather than a fix for a frequent failure. |
| Word "attorney sign-off required" banner | Low | Fix #4 added `report.requires_attorney` (the authoritative server verdict). Word already renders blockers client-side via `deriveBlockers`, so behavior is covered; a small banner in FindingsTab consuming the new flag would make the verdict explicit. Sideload smoke-test required. |
| Gate generation/drafting interrupt on `interactive_review` | Low | Fix #4 gates only the **contract_review** interrupt on the new capability flag. `contract_generation` / `drafting` still route to `human_review` unconditionally — safe today because those are Chainlit-only flows (Chainlit sets `interactive_review=True`), but a non-interactive caller doing generation would strand the session. Extend the flag-gate to those branches when a second non-interactive client surfaces. |
| RAG chunker: depth-1-numbered clause loss (`parse_docx`) | Medium | The SAME root cause Fix #5 routed around for the review path still affects **RAG ingestion**: `docx_parser._parse`'s `num_depth==1` branch treats any decimal-numbered paragraph as a section heading and discards its body. Mild on the policy corpus (3 long clauses lost across 52 files; numbered items there are short headings) but **mutilates any contract `.docx` ingested via `/api/ingest`**. Fix: only treat a numbered paragraph as a section title when heading-like (bold first run, or ≤~8 words); otherwise preserve the body (as depth-2+ already does). Refactor the decision into a unit-testable `_is_section_heading(para, num_info)` helper. **Must ship behind a regression harness** asserting per-file coverage across the 52 policy docs stays ≥ current, then **re-ingest** the corpus. PDF (`parse_pdf`) has separate section logic — audit separately. |
| Word add-in: playbook-grounded findings (Phase 2) | High | Make `contract_review` emit structured per-finding JSON including the retrieved RAG chunks; pane shows "your firm's standard says X" next to "this contract says Y" with source citations. The real Spellbook differentiator. Now that the team's matrix is in-prompt the LLM can already cite per-clause IDs (NDA-001, MSA-014…); JSON output would let the pane render them as live links. |
| psycopg_pool reconnection noise during a Postgres outage | Low (new 2026-08-18) | Observed during the `fix/postgres-degradation` smoke. While `app-db` is down, psycopg_pool logs a ~6-line `error connecting in 'pool-1'` block per reconnection attempt, continuously and independent of traffic, plus `reconnection attempt in pool 'pool-1' failed after 300.0 sec` on a 5-minute cycle. **Not a regression** — the backend always stayed alive through an outage (only the turns died), so this noise predates the fix; it is simply now visible for the whole outage instead of being cut short by an operator restarting things. Harmless locally, but an overnight `app-db` outage on the VM would write a lot of log carrying no new information and could bury a genuine error. Deliberately **not** silenced in that branch: during an outage the noise is real diagnostic signal, and choosing a log level for someone else's incident is an operator decision, not a code default. If it becomes a problem the lever is a level on the `psycopg.pool` logger, or a larger `reconnect_timeout`. Revisit after the first VM outage shows what the volume actually looks like. |
| **Postgres down = a dead chat turn (Redis has a degrade path, Postgres does not)** | ✅ **SHIPPED 2026-08-18** (`fix/postgres-degradation`) | Found by the tester-feedback sideload smoke, and **pre-existing — not that branch's doing** (the `/api/query` path never touches the feedback tables; both reads below predate it). With `app-db` stopped, a doc-chat turn hangs **30 s** and then dies in the pane with `Error: couldn't get a connection after 30.00 sec` — `psycopg_pool`'s `PoolTimeout`. A chat turn reads Postgres *before* it ever calls the model: [skills/legal_research/context.py:62](skills/legal_research/context.py) `load_latest_review` and [:97](skills/legal_research/context.py) `load_recent`, plus `memory_writer`'s append on the way out. **The asymmetry is the bug.** A **Redis** failure is handled deliberately and well — `_get_stateless_graph` + `_is_redis_failure` degrade the run, the turn still answers, and `memory_degraded` raises an amber banner. **Postgres has no equivalent**, even though a chat turn does not need it: the document arrives in the request and recall is an *enhancement*. The graceful behaviour is the one already built for Redis — skip recall, answer anyway, set `memory_degraded`. Instead the attorney waits half a minute for a failure. On the VM this is the difference between "memory is flaky today" and "the tool is broken". Fix has two parts, and the first is worth doing alone: (a) **bound the wait** — `memory/db.py`'s `ConnectionPool` takes no `timeout=`, so it inherits the 30 s default; a 2-3 s timeout turns a stall into a fast, correct degrade; (b) **wrap the recall reads** (`_load_prior_review_block`, `_load_prior_conversation`) and `memory_writer`'s append so a store outage sets `memory_degraded` instead of raising — the conversation append is already best-effort at its call site, so this is mostly the read path. Note the reviews-write must stay LOUD (`review_persist_error`); it is a legal record, unlike recall. Deliberately **not** fixed inside `feat/tester-feedback-capture`: unrelated blast radius (graph + skills, which that branch is constrained never to touch) and it wants its own Redis-down/Postgres-down smoke. **⚠️ CORRECTION made while fixing it — part (b) above was wrong.** The two recall reads were **already** wrapped and already set `memory_degraded` ([context.py:61-66](../skills/legal_research/context.py), [:96-103](../skills/legal_research/context.py)); wrapping them would have changed nothing. The actual raiser was the one store call nobody had wrapped: **`write_audit_log`** at the top of `memory_writer`, which runs on **every** turn — so this killed contract review exactly as it killed chat, not just doc-chat as diagnosed. **What shipped:** (a) as designed — `ConnectionPool(timeout=config.db_pool_timeout)`, default 3.0s, plus an integer `connect_timeout` floored at 2 (libpq treats 1 as 2) so a blackholed host can't leave background workers hanging; this alone fixes *every* Postgres caller, including the already-wrapped reads that previously burned 30s before their `except` fired. (b) `write_audit_log` wrapped: the full entry goes to the app log at ERROR and the turn is flagged `memory_degraded`. Constraint 6 was reworded to state that relaxation explicitly. `memory_writer` restructured to one return so an audit failure and a review failure can no longer hide each other (the old early return could only report whichever came second). **Non-obvious constraint for anyone touching this:** `memory_writer` runs AFTER `output_formatter`, so the flag must ride the **returned report** — setting `state["memory_degraded"]` there passes every node-level test and never reaches the banner; `tests/test_query_memory.py::test_memory_writer_degradation_reaches_the_payload` drives the real node into the real payload builder to pin that seam. `init_db()` at lifespan stays fatal by design (compose gates the backend on `app-db: service_healthy` and restarts it), so "backend boots with app-db down" is not a degrade case — it is a refusal to boot. |
| Word pane: no timeout or queue feedback on a slow turn | Medium (new 2026-08-14) | The pane shows a fixed `Thinking… (30–60 s)` with no timer, no timeout and no way to cancel, so on the VM — where Ollama at `172.20.0.22` is **shared with compliance-bot** and serial by default — waiting behind another tenant is visually identical to a broken backend. The backend waits patiently (httpx timeout 600 s) and the turn does eventually complete, but the attorney has no way to know that. Backend-side visibility is now fixed (see the in-flight logging Shipped row); the **client** side is not. Options: show elapsed time once past ~60 s, surface a "still working / queued" state, add a cancel affordance, or have the backend report queue position. Worth pairing with a decision on `OLLAMA_NUM_PARALLEL` on Spark — serial execution across two tenants is the root condition. |
| Chat: pronoun/referent resolution in edit targets | Medium (new 2026-08-13) | From VM conversation `2ae99ecc`: `"fill legal name with it"` produced `{"action":"replace","target_text":"[Legal Name]","new_text":"it"}` — the literal word "it" written into the counterparty legal name, as a one-click Apply. The referent ("Acme Corp") was three turns earlier in the same conversation and the model had just recalled it correctly when asked directly. Same family as the fabricated-address row: a `new_text` that is not a plausible field value. A provenance guard scoped to *facts* would not catch this one ("it" is not entity-shaped), so it likely wants its own check — reject `new_text` that is a bare pronoun/article for a named-field target. Cheap and deterministic. |
| Chat: spurious edit proposals on factual questions | **High (re-rated 2026-08-14 — clearest repro yet, on the VM)** | **The no-op half is fixed** (`isNoOpEdit`, 2026-08-13); this is the substantive half, and it now has an unambiguous reproduction. Asked the pure question **"who signs?"**, the model answered correctly — *"The document does not specify who signs; the signature block contains unfilled placeholders for the signatory's name and title"* — then emitted **three unrequested edit cards** filling those very placeholders, contradicting its own answer in the same reply. **The rationale names the mechanism outright:** *"User **previously** requested to fill the signed by field with Suzy Qutreo."* It is not answering the current turn; it is completing work it believes is still pending. **History sanitisation does NOT fix this, contrary to expectation.** `_sanitize_history` (shipped 2026-08-13) strips ```json``` blocks from replayed turns, but the PROSE survives — *"I will update the signature block to insert Suzy Qutreo as the signatory"* — which is a record of intent. Combined with a document still showing `[__]` (the preceding turn degenerated into a repetition loop and applied nothing), the model concludes the work is outstanding. Stripping the machinery removed the *format* example and left the *intent*: it fixed preference priming, where the block IS the pattern, and does nothing where prose carries the instruction. **Candidate check, deliberately NOT built:** `_looks_like_edit_promise` already exists, so the symmetric rule is *edits emitted with no edit-promise in the prose = unrequested*. It fires cleanly here (pure answer, no promise). But it would also fire on the prompt's OWN worked example — *"Sure — here's a 2x cap for Section 5"* is a legitimate edit turn whose prose does not match the promise pattern — so it could only ever be a **badge**, never a drop, and its worth depends entirely on how often terse-but-legitimate phrasing trips it. **That is a rate question, not a design question.** Six guards shipped across 2026-08-13/14; this one's false-positive profile is unknown, so it waits on the eval harness instead of becoming the seventh. Logged by decision, not oversight. **Denominators now exist (2026-08-14):** `edits_proposed` (count in `detail`) against `edit_discarded` (`scripts/feedback_report.py` / `event_counts()`) gives a real base rate for how often edits are proposed and then thrown out, and the ⚑ on a card lets an attorney flag this exact contradiction — the badge-vs-drop decision above can now be made from data instead of one repro. |
| **Promote a feedback row to an eval fixture (Phase A)** | Medium | A flagged `feedback` row already carries a replayable `snapshot` (document text, assistant output, the request) plus `turn_id`/`trace_id` — everything an eval case needs except a scored expected answer. Build a script that takes a `feedback.id`, writes its snapshot out as a fixture paired with the attorney's own comment as the known-bad description, and seeds the eval harness's case list from real testing instead of the hand-picked traces (`cc81804f`, `0f63a143`, `4b24ca1d`, …) used so far. Turns "a tester complained" directly into a regression test — the loop `scripts/feedback_report.py` only opened. **The target format now exists** (`evals/cases/<kind>-<slug>.json`, `feat/eval-harness`, 2026-08-20) — `kind: "apply"` maps directly onto a flagged edit-failure row (paragraphs + proposal + expected `Result`), so a promoter targeting `feedback.id` → case file has a real schema to write into rather than a hypothetical one. Still blocked on tester volume — ~5 feedback rows and one real `edit_failed` row exist today, not enough to justify the script. |
| **Eval harness Tier 2 (LLM-scored)** | High | The Tier 1 harness (`feat/eval-harness`, 2026-08-20) is deterministic and zero-LLM by design — no judge model, no rubric, no improvisation-rate or ceiling-adherence scoring. That is deliberately everything model-behavior: playbook-ceiling violations (roadmap: "Playbook-adherence (ceiling) violations"), the fabricated-address / spurious-edit / pronoun-referent traces (all explicitly excluded from the Tier 1 corpus as "model behavior, not machinery"), and — highest-stakes — the **relaxing-preference safety gate** (see that row above), which is the one piece the self-improving-harness north star's "guardrail" leg is still missing before Stage 2 can be trusted. Tier 2 wants a judge/rubric plus a **closed taxonomy** derived from real feedback (see "Derive the taxonomy…" row below) before it can be built well — scoring against guessed categories now would bake in the wrong ones. Model comparison (roadmap: "LLM evaluation across models") and the Langfuse prompt-versioning idea both wait on this too. |
| **Calibrate the Word fake against `edit_failed` rows** | Low | The Tier 1 fake (`clients/word/src/eval/wordFake.ts`) is falsifiable by production today — a real `edit_failed` `interaction_event` row is ground truth, and a fake match where production reported `Couldn't find "…"` would mean the fake (and the `CLAUDE.md` gotcha it encodes) is wrong. Right now that check is manual and ad hoc. Worth automating as a scheduled job once there is a stream of `edit_failed` rows instead of the single one on record (2026-08-18) — pull recent rows, replay each `target_ref` against the fake, and flag any the fake would have found. Waits on tester volume, same as the feedback-promoter row above. |
| **Eval harness (Tier 1) — known limitations** | Low | Four small gaps surfaced while building the harness, none worth fixing yet. **(1)** `evals/baseline.json` cannot scope a known failure to one runner — a Python-only failure on a `parse` case would be misreported by the TS runner as an "unexpected pass" (the id is in the baseline, the TS side never fails it, `[now-passing]` fires wrongly). Dormant today since the baseline is `{}`. `parse` is the only kind both runners share, so a genuine `parse`-only finding is by definition one-sided: the *first* `parse` finding that ever earns its keep is unbaselineable, and `check.sh` would stay red until the underlying bug is fixed instead of being able to baseline around it — the expected first use of this mechanism, not a speculative edge case. **(2)** `wordFake.ts`'s `occurrences()` advances by one character, so it counts *overlapping* matches where real Word's Find probably does not — an untested latent fidelity gap, not yet exercised by any case. **(3)** Neither runner validates case shape: a malformed `evals/cases/*.json` file raises a raw `KeyError`/`JSONDecodeError` (Python) or reports a bare `[FAIL]` with no diagnostic (TS, `runner.ts` on a case missing `input.proposal` or `expect.edits`) instead of a per-case error. Harmless with today's hand-authored corpus; worth a guard once the feedback→fixture promoter (above) is writing cases unsupervised. **(4)** One small fake internal is a latent footgun, not a bug: `applyMutation` throwing mid-`sync()`-batch (the tracked-deletion-overlap refusal) leaves `pendingMutations` uncleared, since the reset sits after the loop — no current test chains `Word.run` calls across a caught throw, but the next behavior added to the fake should check this before building on top of it. (`getRange` no longer has a matching footgun: an unsupported `RangeLocation` now throws instead of silently returning the start point, matching how `insertText` already treats an unsupported `InsertLocation`.) |
| **Derive the taxonomy from the first ~30 items** | Medium | `feedback.comment` is free text with no category field, by design — an attorney filing a complaint shouldn't have to classify it first. Once ~30 real rows exist, read them and derive a small closed taxonomy (fabrication, missed placeholder, wrong clause matched, over-reach/scope, tone, other) after the fact rather than guessing categories up front and steering testers into the wrong one. Feeds both the Phase A fixture work above and the rate work the six annotated rows below are waiting on — a taxonomy is what turns a pile of comments into a denominator **per failure mode**, not just a total. |
| **`interaction_event` retention** | Low | Same shape as the `conversation_store` retention row below, and for the same reason — the table has no pruning policy and grows with every Apply/Discard/failure. Bundle the two into one retention pass when either grows large enough to matter. `feedback` is deliberately excluded from this: a written report behaves like `review_store` (kept), not like a log line (prunable). |
| **The attorney's own tracked changes as ground truth** | Medium | Every tracked change an attorney makes in Word after the assistant's turn — accept, reject, or their own manual rewrite — is a stronger implicit verdict than a flagged `feedback` row: it's what they actually did, not what they bothered to write down, and it exists whether or not anyone files a report. Not yet captured — this branch instruments Apply/Discard on OUR proposals, not what happens to the document afterward. **Baseline-ambiguity caveat:** Word's revision model doesn't attribute a later edit to "in response to which suggestion" — a manual change after an Apply could mean the redline was wrong, or could be an unrelated edit to a different clause entirely. Needs a proximity/overlap heuristic (same clause, edited within N minutes of the Apply) before it's usable signal, not a straight diff. |
| Cloud / hybrid inference + tool-calling grounding agent | Medium (needs privacy sign-off) | The current local qwen3.6 makes a full-grounding chat turn ~30s and never reuses the prefix cache, forcing the conditional-grounding workaround. A fast cloud model (Ollama Cloud / API) would dissolve both: cheap prefill makes always-grounding fine, AND a **tool-calling agent that fetches "skills where needed"** becomes viable (the ReAct path was removed locally only because it was multi-minute). BUT this is a legal tool — sending client contracts + the MSA to a cloud endpoint is an **attorney-client-privilege / data-residency decision**, not just perf; the stack is deliberately local-first. Treat as its own initiative with a firm sign-off before any code. |
| Ollama cross-turn prefix-cache reuse doesn't engage | Low | The grounding is ordered stable-first specifically so Ollama can reuse the ~13.6k-token prefix across chat turns, but traces show it re-prefills the full prompt every turn even with `OLLAMA_NUM_PARALLEL=1` + `KEEP_ALIVE=-1`. Conditional grounding is the current workaround. If reuse can be made to engage (model chat-template / llama.cpp cache behavior), always-grounding would become cheap and the gate could be relaxed. Low priority — the gate works. |
| Chat smoke: MSA-primary + BAA documents | Low | `feat/chat-memory-grounding` was live-smoked on **SOW + NDA** (both grounding code paths: playbook+MSA and playbook-only). MSA-as-primary-doc and BAA follow the same playbook-only path as NDA (detect type → load bundle → no MSA-attach), so they exercise no new code — but haven't been run. Smoke for completeness when convenient. |
| Chat path: surface detected contract-type on the trace | Low | `_build_chat_grounding` detects contract-type internally (to pick the bundle + decide SOW→MSA) but, unlike the review path, doesn't write it to Langfuse trace metadata — so auditing which bundle a chat turn used means inferring from the doc text. A one-line `langfuse_context.update_current_trace(metadata=…)` in the chat grounding builder would close the gap. |
| Structured-JSON review output / selective finding injection | Medium (measured-need) | `contract_review` currently persists and injects the full markdown review into chat context. Structured JSON per-finding would let the chat path inject only the findings most relevant to the question (retrieval-narrowing) and enable Phase 2 playbook citations in the Word pane ("your firm's standard says X"). Defer until recall gaps are measured on real docs. |
| Clause segmentation / retrieval-narrowing on chat | Medium (measured-need) | Chat context currently injects the full uploaded document (capped at `config.chat_context_max_chars` by truncating the doc). For long contracts a clause-segmentation + retrieval step (fetch the clauses most relevant to the chat question) would shrink the context and improve focus without losing grounding. Measure first: compare answer quality full-doc vs retrieved-clauses on the demo SOW/MSA. |
| FTS cross-matter precedent recall on the Postgres review store | Low (measured-need) | `review_store.py` appends one row per review session; today only the latest review per `document_id` is recalled in chat. Full-text search across all stored reviews (same `client_id`) could surface "we flagged this same IP clause Red in three prior SOWs" — cross-matter precedent. Build when the store has enough history to make it useful. |
| O365 SSO attorney identity | Medium | Slice 3 — **backend foundation DONE** (`feat/o365-sso-identity`): `api/auth.py` `resolve_user_id` validates an `Authorization: Bearer` O365 JWT → `state["user_id"] = oid`, config-gated by `sso_enabled` (default False = today's `X-User-ID` path). See the Shipped row. **Remaining (deferred until a live Azure app):** the SSO-on **client** wiring — Office `getAccessToken()`, manifest `WebApplicationInfo` (app id + `api://…` resource), attaching the Bearer header, and the MSAL dialog fallback for legitimate `getAccessToken()` failures — plus the Azure app registration + admin consent. Also: conversation-history migration at cutover (localStorage id → oid re-keys the `attorney_id` partition). |
| Chat gate-verdict reconciliation: broaden trigger to prose gates | Low (measured-need) | The shipped gate reconciliation fires only when the No-Signature gate **literally quotes** a dropped placeholder token. Real qwen reviews write the gate in **prose** (e.g. "Unfilled placeholders in Preamble … and Execution block"), so it rarely fires in practice — fails safe (never misbehaves). Word smoke (2026-07-21) also showed the model's current-document grounding already handles the visible behavior (it did not parrot a filled `Signed by:` as unfilled). Broaden the trigger (annotate when a row drops **and** the gate carries placeholder/unfilled language; neutralize still only when 0 Red/Missing-Context findings survive) **only if** stale-gate parroting is actually observed with a real model. |
| Chat: re-review nudge on doc divergence (stale-recall, next slice) | Medium | Follow-up to gate-verdict reconciliation. When the current doc diverges *beyond* blank-fills (real clause edits) since the stored review, nudge a re-review. Blocked on a prerequisite: the review store holds only markdown + contract_type, **not a doc fingerprint at review time**, so "diverged beyond blank-fills" isn't computable yet — persist a doc fingerprint (normalized hash) with each stored review, then diff on recall. Reconciling *substantive* findings via clause-level retrieval (needs structured-JSON review output) stays deferred too. |
| Conversation retention / pruning policy | Low | `conversation_store` grows unbounded; only the injected window (`conversation_max_messages`) is capped. Revisit if the Postgres `conversation_store` table grows large enough to matter. |
| Word add-in: bulk actions ("Apply all RED") | Medium | Findings tab — one click to track-change every RED finding, plus a stale-findings banner after the doc is edited. (Filter/sort pills **shipped** in `feat/word-addin-quick-ux`; click-to-jump navigation shipped there too.) |
| Word add-in: generate-clause tab (Phase 6) | Low | Mostly subsumed by Iteration 3's chat-driven inserts; a dedicated tab is now optional polish rather than the primary path. |
| Word add-in: AppSource publishing | Low | Sideload-only for now; publish to AppSource when ready for outside attorneys. Needs proper icons + manifest signing. |
| WebSocket / SSE streaming | Medium | LLM responses arrive all at once; would improve perceived latency on long generations |
| Admin API endpoints (sessions, skills, reviews, audit) | Medium | Deferred until a UI consumes them |
| Long-term memory (Qdrant `memory` collection) | Medium | Cross-session attorney preferences; implement when usage patterns emerge |
| DOCX output generation | Medium | PDF works; DOCX needed for Word add-in round-tripping (attorney edits doc in Word → exports back as DOCX) |
| Real authentication | Medium | `X-User-ID` today carries a per-install attorney UUID (partitioning key, not auth); real auth needed before the multi-user VM. The Word path's **backend** is now shipped (dormant `resolve_user_id` SSO seam, `sso_enabled=False`); turning it on needs the deferred SSO-on **client** wiring + an Azure app — tracked under **O365 SSO attorney identity** (slice 3) above. |
| Remaining skills as folders | Low | `compliance_check`, `legal_research`, `drafting` still flat files |
| MCP tools integration | Low | Architecture supports it — tools plug into agent registries |
| Langfuse prompt versioning | Low | Prompts logged per-call; formal versioning not set up |
| Langfuse per-model cost + ReAct span fidelity | Low | GENERATION spans now carry token usage, but **cost** is null until a model price for `qwen3.6:latest` (and any eval models) is registered in the Langfuse UI (Settings → Models). Also: `traced_agent_invoke` records each ReAct run as one outer GENERATION (input + final output + summed usage); per-internal-turn/tool spans would need the LangChain callback handler, which needs the full `langchain` package (deliberately not a dep). |
| Skill-span enrichment (timing-only today) | Low | By design the five skill spans set `capture_input/output=False`, and `contract_review`/`drafting`/`compliance_check` make no LLM call (they assemble the prompt for `llm_caller`), so their spans are **intentionally bare timing markers** — not a bug. The verbatim generation + token usage live on the downstream `llm_caller` GENERATION. Optional enrichment: attach small metadata per skill (contract_review → `contract_type_detected`/ambiguous/doc-size; legal_research → path + #edits) without re-enabling state capture. Verified empty on trace `c8cf747b`. |
| Trace/Redis bloat: full-state dumps on node spans | Medium | The non-skill nodes (`intake`, `rag_retriever`, `risk_assessor`, `output_formatter`, `history_appender`, `memory_writer`) auto-capture the entire `LegalAgentState` as both input and output — it snowballs ~14k→66k chars across the pipeline, captured ~2× per node (trace `c8cf747b`). Fix: set `capture_input/output=False` on those nodes (the explicit prompt/response I/O on `llm_caller`/`intent_router`/`planner` survives because it's set via `update_current_observation`). Bundles with the existing latency/bloat follow-up. |
| SOW-vs-MSA review: scale beyond the one-MSA demo | Medium | `get_parent_msa` currently returns "the one MSA on file for this `client_id`" (picks first by `doc_id` when >1). To scale: match the *specific* parent MSA by party name or an explicit MSA reference inside the SOW (`get_parent_msa` still picks the first MSA by `doc_id` for the `client_id`); RAG-select only the MSA clauses relevant to the SOW (instead of full text) when MSAs exceed the cap. **DONE:** the cap is now `config.msa_max_chars` (config.py:62, default 24000), shared by both the review and chat paths — the inline `_MSA_MAX_CHARS` constant is gone. The structural comparison directive could also be promoted into the SOW `SKILL.md` source (legal-team-owned) once the team wants to author formal MSA-vs-SOW rules. |
| Policy chunking strategy verification | Low | TBD — test clause-based vs section-based |
